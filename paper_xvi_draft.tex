\documentclass[12pt,a4paper]{amsart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{enumitem}
\usepackage{hyperref}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{problem}[theorem]{Open Problem}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\Rig}{\textup{[Rig]}}
\newcommand{\Cond}{\textup{[Cond]}}
\newcommand{\Open}{\textup{[Open]}}

\begin{document}

\title[Lagrangian Singularity Transport under Fourier Duality]{%
  Lagrangian Singularity Transport under Fourier Duality:\\
  Cubic WKB Phases, A$_2$-Fold Singularities,\\
  and the Airy Normal Form}

\author{[Author]}

\begin{abstract}
A cubic WKB phase $c_3(c)\beta^3$
produces a degenerate stationary point
of order~2, which is an A$_2$-fold
singularity of the canonical relation
$\mathcal{C}$.
The Airy distribution is the universal
local normal form of this singularity
under FIO conjugacy.
Three results are established,
each explicitly conditional on
Assumption~\ref{ass:microlocal}
(uniform A$_2$-stability,
conditions (i)--(vi)):
\begin{enumerate}[\rm(I)]
\item \textit{(Local Airy normal form.)}
  Theorem~\ref{thm:airy-normalform}:
  FIO conjugacy yields an Airy model
  integral for $\Phi$,
  with amplitude $a\in S^{-1/2}_{1,0}(c)$
  and rapidly decaying remainder $r$
  (conditional on (i)--(vi)).
\item \textit{(Pointwise decay.)}
  Corollary~\ref{cor:pointwise}:
  $|\Phi(u;c)|\le C(1+|u|)^{-3/4}$
  uniformly for all $u$
  (conditional on (i)--(vi)).
\item \textit{(Ces\`aro improvement.)}
  Corollary~\ref{cor:global-bound}:
  integrability improvement
  $|\int_0^U\Phi|=O(U^{1/4})$,
  conditional on (i)--(vi).
\end{enumerate}
The forward direction is rigorous
under (i)--(vi);
the converse requires the non-standard
Hypothesis~\ref{hyp:FIO-rigidity}.
\end{abstract}

\maketitle

\section{Setup and Definitions}
\label{sec:setup}

\begin{definition}[A$_2$-fold singularity]
\label{def:A2-class}
Let $\mathcal{C}\subset T^*\mathbb{R}_\beta
\times T^*\mathbb{R}_u$ be a Lagrangian
submanifold,
$\pi_u:\mathcal{C}\to T^*\mathbb{R}_u$
the projection to the right factor.
\medskip

\noindent\textit{Geometric definition.}
$\mathcal{C}$ has an
\emph{A$_2$-fold singularity at $p_0$} if:
\begin{enumerate}[\rm(i)]
\item $\det\,d\pi_u\big|_{p_0}=0$;
\item $d(\det\,d\pi_u)\big|_{p_0}\ne 0$.
\end{enumerate}
This is the A$_2$-type in Arnold's
classification \cite{Arnold1972}.
\medskip

\noindent\textit{Coordinate reduction.}
For phase $\Psi(\beta,u)
=\Theta(\beta;c)+u\beta$,
conditions (i)--(ii) reduce to
$\Theta''(0;c)=0$,
$\Theta'''(0;c)\ne 0$.
These are coordinate expressions;
conditions (i)--(ii) are invariant
under canonical transformations.
\end{definition}

\begin{remark}[FIO conjugacy, not
  phase identity]
\label{rem:FIO-language}
Throughout this paper,
statements of the form
``the phase is reduced to Airy
normal form''
always mean:
\emph{FIO conjugacy yields an Airy
model integral},
i.e.\ the oscillatory integral for $\Phi$
is conjugated, via a local canonical
transformation $\beta\mapsto t(\beta,u;c)$,
to an integral whose phase is the
standard Airy phase
$\frac{1}{3}t^3-\mu(u;c)t$.
This is \emph{never} a pointwise
phase identity
$\Theta(\beta;c)+u\beta
=\frac{1}{3}t^3-\mu t+R$.
The Thom--Boardman theorem
\cite{Golub1973} guarantees
this conjugacy locally;
the amplitude carries all
remainder contributions.
\end{remark}

\begin{remark}[Microlocal transport]
\label{rem:microlocal-transport}
The Fourier transform acts by the
exchange
$(\beta,\xi;u,\eta)\mapsto(u,\eta;\beta,\xi)$.
If the Lagrangian map germ of $\pi_u$
belongs to the stable A$_2$-class
(Mather stability \cite{Golub1973}),
then the germ of $\pi_\beta$ also
belongs to the stable A$_2$-class.
This is a local statement;
it fails if $c_3(c)\to 0$.
\end{remark}

\subsection{The fold amplitude and
  conormal singularity}

The fold amplitude is:
\begin{equation}\label{eq:Phi-WKB}
  \Phi(u;c)
  = e^{i\alpha_* u}
  \int_{\mathbb{R}}
  |\beta|^{-1/2}
  e^{i(\Theta(\beta;c)+\beta u)}
  \chi(\alpha_*+\beta)
  \,d\beta,
\end{equation}
where $\chi\in C^\infty_c$,
$\alpha_*>0$,
$\theta''(\alpha_*;c)=0$,
$c_3(c):=\frac{1}{6}\theta'''(\alpha_*;c)\ne 0$,
$\Theta(\beta;c):=\theta(\alpha_*+\beta;c)$.
\medskip

\noindent\textbf{Conormal singularity.}
The weight $|\beta|^{-1/2}$ is not
locally integrable at $\beta=0$;
the integral~\eqref{eq:Phi-WKB} is
interpreted as a distributional
oscillatory integral.
The amplitude
$a_0(\beta):=|\beta|^{-1/2}\chi(\alpha_*+\beta)$
belongs to the class of
\emph{conormal distributions}
with singularity at $\beta=0$:
\[
  a_0\in I^{m}(\mathbb{R},\{0\}),
  \quad m=-1/4,
\]
(H\"ormander \cite{Hormander1983},
Ch.~18.2;
Guillemin--Uhlmann
\cite{GuillemU1981}).
All stationary-phase and FIO operations
belong to this distributional setting.

\begin{theorem}[Endpoint decay \Rig]
\label{thm:endpoint}
$|\Phi(u;c)|=O(|u|^{-1/2})$
for all $\theta\in C^\infty$,
uniformly in $c$.
\end{theorem}

\begin{proof}
The amplitude $a_0\in I^{-1/4}(\mathbb{R},\{0\})$
has a conormal singularity at $\beta=0$;
all operations are distributional.
For $\theta\equiv 0$,
Erd\'elyi \cite{Erdelyi1956} gives
exact asymptotics.
For general $\theta$,
van~der~Corput's lemma
applied in the distributional
sense (H\"ormander \cite{Hormander1983},
Ch.~7.7) gives $O(|u|^{-1/2})$.
\end{proof}

\section{CFU Reduction, Conormal Stability,
  and Symbol Classes}
\label{sec:reduction}

\begin{assumption}[Local stable A$_2$-fold]
\label{ass:fold-local}
$\Theta(\beta;c)=c_3(c)\beta^3+O(\beta^4)$,
$c_3(c)\ne 0$, and:
\begin{enumerate}[\rm(F1)]
\item $\Theta''(\beta;c)\ne 0$
  for $\beta\ne 0$ near $0$.
\item The oscillatory integral
  $\int|\beta|^{-1/2}
  e^{i\Theta(\cdot;c)}\chi\,d\beta
  \not\equiv 0$ near $0$.
\item $\partial_\beta\Theta(\beta;c)$
  has exactly one critical point
  ($\beta=0$) on $\mathrm{supp}\,\chi$.
\end{enumerate}
\end{assumption}

\begin{lemma}[Conormal amplitude
  stability under CFU \Rig]
\label{lem:conormal-stability}
Let $a_0=|\beta|^{-1/2}\chi
\in I^{-1/4}(\mathbb{R},\{0\})$.
Under the CFU canonical change of
variables $\beta\mapsto t(\beta,u;c)$
of Proposition~\ref{prop:CFU},
the transported amplitude
\[
  \tilde{a}(t,u;c)
  := a_0(\beta(t,u;c))\cdot J(t,u;c)
  \cdot\tilde\chi(t,u;c)
\]
remains in the conormal class:
$\tilde{a}(\cdot,u;c)
\in I^{-1/4}(\mathbb{R},\{0\})$
in $t$, for each fixed $(u,c)$;
under condition (vi) of
Assumption~\ref{ass:microlocal},
this holds uniformly in $(u,c)$.
\end{lemma}

\begin{proof}
The map $t\mapsto\beta(t,u;c)$
is a local diffeomorphism
(condition (vi): $|\det(dt/d\beta)|
\ge\delta_5>0$),
with all derivatives bounded
uniformly (condition (vi)).
Pushforward of conormal
distributions under local
diffeomorphisms preserves
the $I^m$ class
(H\"ormander \cite{Hormander1983},
Ch.~18.2;
Guillemin--Uhlmann \cite{GuillemU1981},
Lemma~2.1);
the uniform bounds under (vi)
upgrade from pointwise to
uniform-in-$(u,c)$.
The Jacobian $J\in C^\infty$ with
all derivatives bounded (condition (vi)),
so multiplication preserves $I^m$.
\end{proof}

\begin{proposition}[FIO conjugacy to
  Airy model integral \Rig]
\label{prop:CFU}
Under Assumption~\ref{ass:fold-local}
(see also Remark~\ref{rem:FIO-language}),
fix $U_\epsilon=\{|\beta|<\epsilon\}$
and a smooth partition
$1=\phi_{\mathrm{fold}}
+\phi_{\mathrm{far}}$.
\begin{enumerate}[\rm(1)]
\item \textit{(A$_2$-fold.)}
  $\Theta''(0;c)=0$,
  $\Theta'''(0;c)=6c_3(c)\ne 0$.
\item \textit{(FIO conjugacy.}
  Recall Remark~\ref{rem:FIO-language}:
  this is never a pointwise
  phase identity.)
  The CFU theorem \cite{CFU1957}
  provides a smooth local map
  $\beta\mapsto t(\beta,u;c)$
  such that FIO conjugacy
  yields the Airy model integral:
  \begin{align}\label{eq:CFU-integral}
    &\int_{U_\epsilon}
    |\beta|^{-1/2}
    e^{i(\Theta(\beta;c)+u\beta)}
    \phi_{\mathrm{fold}}\,d\beta
    \\
    &\quad=
    e^{iR(u;c)}
    \int_{\mathbb{R}}
    \tilde{a}(t,u;c)\,
    e^{i(\frac{1}{3}t^3-\mu(u;c)\,t)}
    \,dt,\notag
  \end{align}
  where $\mu(u;c)=c_3(c)^{-1/3}u
  +O(u^2)$, $R(u;c)$ smooth,
  and $\tilde{a}\in I^{-1/4}
  (\mathbb{R},\{0\})$
  (Lemma~\ref{lem:conormal-stability}).
  This is an \emph{exact identity
  for the oscillatory integral}.
\item \textit{(Far region.)}
  $|\Phi_{\mathrm{far}}(u;c)|
  \le C|u|^{-1/2}$ by van~der~Corput.
\end{enumerate}
\end{proposition}

\begin{lemma}[Jacobian rescaling \Rig]
\label{lem:Jacobian-rescaling}
Under the canonical Airy rescaling
$t = c_3(c)^{-1/6}|u|^{1/2}\tilde{t}$:
\begin{equation}\label{eq:Jacobi-homog}
  J(t,u;c)
  = c_3(c)^{-1/6}|u|^{-1/2}
  \tilde{J}(\tilde{t},u;c),
  \quad
  \tilde{J} = 1+O(|u|^{-1}).
\end{equation}
The $|u|^{-1/2}$ factor arises from
the homogeneity of the cubic normal
form, not from $|\beta|^{-1/2}$.
\end{lemma}

\begin{proof}
Cubic Airy phase becomes
$c_3(c)^{-1/2}|u|^{3/2}
(\frac{1}{3}\tilde{t}^3
-\mathrm{sgn}(u)\tilde{t})$
under the rescaling;
$J = \tilde{J}\cdot c_3(c)^{1/6}
|u|^{-1/2}$
from $J\,dt=d\beta$;
$\tilde{J}=1+O(|u|^{-1})$
by CFU \cite{CFU1957}.
\end{proof}

\begin{lemma}[Parameter-dependent
  symbol stability \Rig]
\label{lem:symbol-stability}
Under Assumption~\ref{ass:microlocal},
define the parameter-dependent
symbol class:
\begin{equation}\label{eq:symbol-class-c}
  S^m_{1,0}(c)
  := \bigl\{a(u;c):\,
  |\partial_u^j a(u;c)|
  \le C_{j}(1+|u|)^{m-j}
  \text{ uniformly in }c\ge c_0
  \bigr\}.
\end{equation}
Let
$\tilde{a}(\tilde{t},u;c)
:=|\beta(\tilde{t},u;c)|^{-1/2}
\tilde{J}(\tilde{t},u;c)
\cdot\tilde\chi(\tilde{t},u;c)$
(as in Lemma~\ref{lem:conormal-stability}).
\begin{enumerate}[\rm(1)]
\item \textit{(Amplitude class in
  $(\tilde{t},u)$.)}
  Under condition (vi),
  $|\partial_{\tilde{t}}^j
  \partial_u^k\tilde{a}|
  \le C_{jk}(1+|u|)^{-1/2-k}$
  uniformly in $(\tilde{t},u,c)$.
\item \textit{(Parametric stationary
  phase, H\"ormander Ch.~7.7.)}
  \begin{equation}\label{eq:airy-decomp}
    \int\tilde{a}(\tilde{t},u;c)\,
    e^{ic_3(c)^{-1/2}|u|^{3/2}
    (\tilde{t}^3/3
    -\mathrm{sgn}(u)\tilde{t})}
    d\tilde{t}
    = a_0(u;c)\,
    \mathrm{Ai}(c_3(c)^{-1/3}u)
    + r_0(u;c).
  \end{equation}
  The coefficient $a_0$ is extracted
  from the stationary-phase expansion;
  it is \emph{not} defined by
  dividing by $\mathrm{Ai}$.
\item \textit{(Symbol estimates.)}
  Under condition (vi):
  \begin{align}
    \label{eq:symbol-u}
    a_0(\cdot;c)
    &\in S^{-1/2}_{1,0}(c),\\
    \label{eq:remainder-u}
    |r_0(u;c)|
    &\le C_{N}(1+|u|)^{-N}
    \quad(\text{each fixed }N,
    \text{ uniform in }c).
  \end{align}
\end{enumerate}
\end{lemma}

\begin{remark}[Remainder and uniformity]
Estimate~\eqref{eq:remainder-u}
holds uniform in $c$
\emph{only under} condition (vi).
Without (vi), rapid decay holds
for each fixed $c$ only.
\end{remark}

\section{Main Results}
\label{sec:results}

\begin{assumption}[Uniform A$_2$-stability
  \Cond]
\label{ass:microlocal}
Assumption~\ref{ass:fold-local}
holds uniformly in $c\ge c_0$, with:
\begin{enumerate}[\rm(i)]
\item $\Theta(\beta;c)
  =c_3(c)\beta^3+O(\beta^4)$
  in a $c$-independent neighbourhood;
\item $c_3(c)\ge\delta_0>0$;
\item $|\Theta''(\beta^*(u);c)|
  \ge\delta_1|u|^{1/2}$
  for $\delta_2\le|u|\le c^{\theta_0/3}$;
\item $|\partial_u^k J(t,u;c)|
  \le C_k(1+|u|)^{-1/2-k}$
  uniformly for all $k$;
\item no secondary stationary points
  uniformly in $c$;
\item \textit{(Uniform symbol-class
  regularity; parameter-dependent
  FIO class.)}
  Uniformly in $(\beta,u,c)$:
  $|\det(dt/d\beta)|\ge\delta_5>0$;
  $|\partial_\beta^j\partial_u^k
  t(\beta,u;c)|\le C_{jk}$
  for all $j,k\ge 0$
  (so that $t(\cdot,u;c)$ defines
  a family of canonical
  transformations in the
  parameter-dependent FIO class
  $S^0_{1,0}(c)$);
  and $|\partial_{\tilde{t}}
  (\tilde{t}^3/3
  -\mathrm{sgn}(u)\tilde{t})|
  \ge\delta_4>0$
  outside the Airy critical region.
\end{enumerate}
\end{assumption}

\begin{remark}[Role of (vi)]
\label{rem:vi-role}
Condition (vi) promotes all estimates
from ``fixed $c$'' to
``uniform in $c\ge c_0$''
via the parameter-dependent FIO class
$S^m_{1,0}(c)$
(Definition~\eqref{eq:symbol-class-c}).
Specifically:
\begin{itemize}
\item $|\det(dt/d\beta)|\ge\delta_5$
  ensures Lemma~\ref{lem:conormal-stability}
  (conormal stability under CFU);
\item derivative bounds place
  $t(\cdot,u;c)\in S^0_{1,0}(c)$,
  so transported amplitudes
  stay in $S^m_{1,0}(c)$;
\item non-degeneracy of the
  non-stationary region
  ensures uniform rapid decay of $r_0$.
\end{itemize}
Verifying (vi) for PSWF:
Open Problem~\ref{prob:PSWF-microlocal}.
\end{remark}

\begin{theorem}[Conditional local
  Airy normal form \Rig]
\label{thm:airy-normalform}
\textit{Conditional on
Assumption~\ref{ass:microlocal}(i)--(vi).
FIO conjugacy yields an Airy model
integral (Remark~\ref{rem:FIO-language}).}
For $\delta_2\le|u|\le c^{\theta_0/3}$:
\begin{equation}\label{eq:airy-nf}
  \Phi(u;c)
  = e^{i(\alpha_*u+R(u;c))}\bigl[
  a(u;c)\,
  \mathrm{Ai}\!\bigl(c_3(c)^{-1/3}u\bigr)
  + r(u;c)\bigr],
\end{equation}
where
$a(u;c)\in S^{-1/2}_{1,0}(c)$
(Lemma~\ref{lem:symbol-stability},
requires (vi)) and
$|r(u;c)|\le C_N(1+|u|)^{-N}$
for each fixed $N$ (requires (vi)).
The oscillatory structure of $\Phi$
is entirely encoded in
$\mathrm{Ai}(c_3(c)^{-1/3}u)$.
\end{theorem}

\begin{proof}[Proof sketch]
Fold zone:
Proposition~\ref{prop:CFU}(2)
(FIO conjugacy, exact identity
for the oscillatory integral;
never pointwise phase identity;
conormal amplitude stable by
Lemma~\ref{lem:conormal-stability});
Lemma~\ref{lem:Jacobian-rescaling};
Lemma~\ref{lem:symbol-stability}
(amplitude in $S^{-1/2}_{1,0}(c)$
under (i)--(vi)).
Far zone:
Proposition~\ref{prop:CFU}(3).
\end{proof}

\begin{lemma}[Airy-symbol estimate
  in fold zone \Rig]
\label{lem:product-estimate}
\textit{Valid in the fold zone
after canonical Airy rescaling;
not a global statement.}
For $|u|\ge\delta_2$:
\begin{enumerate}[\rm(a)]
\item $u\ge\delta_2$:
  $|a\cdot\mathrm{Ai}(c_3^{-1/3}u)|
  \le C_1C_2\,u^{-3/4}$.
\item $u\le-\delta_2$:
  $|a\cdot\mathrm{Ai}(c_3^{-1/3}u)|
  \le C_1C_2\,|u|^{-3/4}$.
\end{enumerate}
The exponent $-3/4$ is the
\emph{joint scaling factor of the
full asymptotic expansion}
after CFU reduction and Airy rescaling:
$|u|^{-1/4}$ (Airy) and $|u|^{-1/2}$
(symbol) arise from a single
stationary-phase expansion in
$\tilde{t}$, not from two independent
mechanisms.
\end{lemma}

\begin{corollary}[Pointwise decay
  \Cond]
\label{cor:pointwise}
\textit{Conditional on
Assumption~\ref{ass:microlocal}(i)--(vi).}
There exists $C>0$,
uniform in $c\ge c_0$
and across all three zones, such that:
\begin{equation}\label{eq:pointwise}
  |\Phi(u;c)|\le C(1+|u|)^{-3/4}
  \quad\forall u\in\mathbb{R}.
\end{equation}
\end{corollary}

\begin{proof}
$C:=\max(C_1C_2,\,C_3,\,C_4)$
from three zones:
\begin{itemize}
\item \textit{Fold zone $|u|\ge\delta_2$:}
  Lemma~\ref{lem:product-estimate}.
\item \textit{Compact zone $|u|\le 1$:}
  compactness + continuity of
  FIO amplitude (condition (vi))
  $\Rightarrow$ $|\Phi|\le C_3$
  (Erd\'elyi \cite{Erdelyi1956}).
\item \textit{Far zone:}
  $|\Phi|\le C_4|u|^{-1/2}
  \le C_4|u|^{-3/4}$
  (Theorem~\ref{thm:endpoint}).
\end{itemize}
Transition ($c^{\theta_0/3}\le|u|
\le 2c^{\theta_0/3}$):
conditions (iii)--(iv)
make constants compatible.
$C$ is uniform in $c\ge c_0$
by (ii) and (vi).
\end{proof}

\begin{remark}[Integrability, not
  oscillatory cancellation]
\label{rem:oscillation}
Corollary~\ref{cor:global-bound}
exploits an \emph{integrability
improvement}:
the absolute-value bound
$|\Phi|\le C(1+|u|)^{-3/4}$
yields $O(U^{1/4})$
by straightforward integration.
No oscillatory cancellation in $u$
is used.
An oscillatory improvement would
require a second stationary-phase
step in $u$; that lies beyond
the scope of this paper.
\end{remark}

\begin{corollary}[Ces\`aro improvement
  \Cond]
\label{cor:global-bound}
\textit{Conditional on
Assumption~\ref{ass:microlocal}(i)--(vi).}
\[
  \left|\int_0^U\Phi(u;c)\,du\right|
  \le C\int_0^U(1+u)^{-3/4}\,du
  = O(U^{1/4}),
\]
improving upon Ces\`aro $O(U^{1/2})$
by integrability gain
(Remark~\ref{rem:oscillation}).
The exponent $-3/4$ is the joint
scaling factor of the full asymptotic
expansion (Lemma~\ref{lem:product-estimate}).
\end{corollary}

\subsection{Converse and FIO rigidity}

\begin{assumption}[Microlocal
  reconstruction (non-standard)
  \Cond]
\label{hyp:FIO-rigidity}
Suppose $\Phi$ is in the Airy class and:
\begin{enumerate}[\rm(a)]
\item $\sigma(g\mapsto\Phi)
  \big|_\mathcal{C}\ne 0$;
\item $g\mapsto\Phi$ is injective
  modulo smoothing;
\item $\mathrm{WF}(\Phi)
  \subseteq\pi_u(\mathcal{C})$
  on the A$_2$ stratum.
\end{enumerate}
Then $\mathcal{C}$ contains an
A$_2$-fold singularity
uniformly in $c$.
\medskip

\noindent\textit{Status.}
This is a
\emph{non-standard microlocal
reconstruction hypothesis
(inverse problem / rigidity conjecture)},
not a consequence of standard FIO
theory \cite{Zworski2012,Hormander1983}.
Verification for PSWF: Paper~XVII.
\end{assumption}

\begin{corollary}[Conditional
  equivalence]
\label{cor:conditional-equiv}
Under Assumption~\ref{hyp:FIO-rigidity}:
Airy-class bound $\Rightarrow$
Assumption~\ref{ass:microlocal}
(modulo the non-standard hypothesis).
The forward direction holds
rigorously without
Assumption~\ref{hyp:FIO-rigidity}.
\end{corollary}

\section{Residual Open Problem}
\label{sec:open}

\begin{problem}[A$_2$-stability
  for PSWF \Open]
\label{prob:PSWF-microlocal}
Does the PSWF amplitude
$g(\alpha;c)=|\alpha-\alpha_*|^{-1/2}
e^{i\theta(\alpha;c)}$
satisfy
Assumptions~\ref{ass:fold-local}
and~\ref{ass:microlocal}
uniformly in $c\to\infty$?
\begin{enumerate}[\rm(a)]
\item $c_3(c)\ge\delta_0>0$
  (Olver turning-point WKB \cite{Olver1974})?
\item $\theta''(\alpha;c)\ne 0$
  for $\alpha\ne\alpha_*$, uniformly?
\item Conditions (iv) and (vi)
  for the PSWF CFU Jacobian?
\end{enumerate}
Outcomes:
(A)~All hold:
$|\int_0^U\Phi|=O(U^{1/4})$.
(B)~$c_3(c)\to 0$:
Ces\`aro sharp.
\end{problem}

\begin{thebibliography}{9}
\bibitem{PaperXV}
[Author], \textit{Paper~XV:
Intermediate-Regime Kernel Estimates},
2026.

\bibitem{Erdelyi1956}
A.~Erd\'elyi,
\textit{Asymptotic Expansions},
Dover, 1956.

\bibitem{Olver1974}
F.~W.~J.~Olver,
\textit{Asymptotics and Special
Functions},
Academic Press, 1974.

\bibitem{Zworski2012}
M.~Zworski,
\textit{Semiclassical Analysis},
AMS, 2012.

\bibitem{Hormander1983}
L.~H\"ormander,
\textit{The Analysis of Linear PDE~I},
Springer, 1983.

\bibitem{CFU1957}
C.~Chester, B.~Friedman, F.~Ursell,
\textit{An extension of the method
of steepest descents},
Proc.\ Cambridge Philos.\ Soc.\
\textbf{53} (1957), 599--611.

\bibitem{Arnold1972}
V.~I.~Arnold,
\textit{Normal forms for functions
near degenerate critical points},
Funct.\ Anal.\ Appl.\
\textbf{6} (1972), 254--272.

\bibitem{Golub1973}
M.~Golubitsky, V.~Guillemin,
\textit{Stable Mappings and
Their Singularities},
Springer, 1973.

\bibitem{GuillemU1981}
V.~Guillemin, G.~Uhlmann,
\textit{Oscillatory integrals
with singular symbols},
Duke Math.\ J.\ \textbf{48} (1981),
251--267.
\end{thebibliography}

\end{document}
