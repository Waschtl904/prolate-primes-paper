\documentclass[12pt,a4paper]{amsart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{booktabs}

%%%─── Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{hypothesis}[theorem]{Hypothesis}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{problem}[theorem]{Open Problem}
\newtheorem{conjecture}[theorem]{Conjecture}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}
\newtheorem*{remark*}{Remark}

%%%─── Macros
\newcommand{\norm}[1]{\|#1\|}
\newcommand{\ip}[2]{\langle #1,\, #2\rangle}
\newcommand{\Phiinf}[1]{\Phi_{#1}^{(\infty)}}
\newcommand{\Phic}[2]{\Phi_{#2}^{(#1)}}
\newcommand{\PW}{PW}
\newcommand{\Kspace}{\mathcal{K}}
\newcommand{\Pc}{P_{c_0}}
\newcommand{\SN}{S_N(c)}
\newcommand{\Bn}{B_N^{(c)}}
\newcommand{\Ac}{A_N^{(c)}}

\subjclass[2020]{47A10, 47B25, 42C05, 46B15, 46C05, 81Q20}
\keywords{Prolate spheroidal wave functions, coefficient stability,
  cross-Gram operator, spectral leakage, Paley--Wiener space,
  no-concentration, local Weyl law, semiclassical measures,
  quantum ergodicity, WKB, spectral evacuation, quadratic forms,
  rank growth, Szeg\H{o} theory, Toeplitz operators,
  double-scaling Szeg\H{o} problem,
  collective quasi-orthogonality,
  near-diagonal cancellation}

\begin{document}

\title[Coefficient Stability of the Slepian Eigenbasis]{%
  Coefficient Stability of the Slepian Eigenbasis:\\
  Cross-Gram Operators, Spectral Evacuation, and the Transition Zone}

\author{[Author]}
\address{[Address]}
\email{[Email]}

\begin{abstract}
The central obstruction to coefficient stability
of the Slepian eigenbasis is
\emph{spectral non-evacuation in the transition
zone}: the persistence of spectral mass of the
measures $\mu_{n,c}(d\xi)
:=|\widehat{\Phi_n^{(c)}}(\xi)|^2\,d\xi$
in the regime $n\sim\alpha c$ as $c\to\infty$.

This paper reduces Hypothesis~H1 of Paper~XIII
(the $\ell^2$-norm convergence of Slepian
coefficients $a_n^{(c)}=\ip{g}{\Phic{c}{n}}$
as $c\to\infty$) to a single spectral
distribution problem in the transition
zone $n\sim\alpha c$.

The key structural results are:
(i)~the cross-Gram operator $T_c$ satisfies
$\norm{T_c}\le 1$ uniformly;
(ii)~the tail functional
\[
  S_N(c) := \sum_{n>N}\mu_{n,c}([-c_0,c_0])
  = \sum_{n>N}\norm{\Pc\Phic{c}{n}}^2_{L^2}
\]
controls the tail via
$\sum_{n>N}|a_n^{(c)}|^2\le\norm{g}^2 S_N(c)$;
(iii)~the operator $\Bn:=(I-\Pi_N^{(c)})\Pc$
satisfies $\norm{\Bn}_{\mathrm{op}}\le 1$;
the inequality $\norm{\Ac}_{\mathrm{op}}\le S_N(c)$
is established via the quadratic-form identity
$\ip{\Ac f}{f}=\lim_M\ip{\Ac^{(M)}f}{f}$,
SOT-convergence, and monotone majorisation
$S_N^{(M)}(c)\nearrow S_N(c)$,
without any global trace-class or
Hilbert--Schmidt assumption;
(iv)~the quadratic form
$Q_c^N(g)=\ip{\Ac g}{g}$
where $\Ac:=\Bn^*\Bn\ge 0$, $0\le\Ac\le I$.

H3 ($\sup_c S_N(c)\to 0$) implies H1.
The gap between quadratic-form convergence
and operator norm convergence is identified
as a \emph{double-scaling Szeg\H{o} problem}
(Definition~\ref{def:double-scaling})
for the truncated Gram operator
$\Ac^{(M)}=P_{V_M^{(c)}}P_{c_0}P_{V_M^{(c)}}$
(\S\ref{sec:szego}).
The resolution requires not merely
pointwise quasi-orthogonality~(D-weak)
but \emph{collective quasi-orthogonality}~(D-strong):
$\|\Ac^{(M)}-D^{(M)}\|_{\mathrm{HS}}\to 0$
for $M(c)\sim\beta c$
(Proposition~\ref{prop:HS-reduction},
\S\ref{sec:PaperXV}).
A structural analysis of the Gram matrix
via WKB asymptotics reveals that the
near-diagonal regime $|n-m|=O(1)$
contributes $O(1/c)=o(1)$ to the
Hilbert--Schmidt sum and is therefore
automatically controlled by the local
Weyl law~(C) alone
(Remark~\ref{rem:near-diagonal-structure}).
The primary obstruction to~(D-strong)
is the \emph{intermediate regime}
$C<|n-m|\le c^{\theta_0}$, where
oscillatory methods require uniform
transition estimates
(Open Problem~\ref{prob:intermediate}).
\end{abstract}

\maketitle

%%═══ 1. INTRODUCTION
\section{Introduction}
\label{sec:intro}

\subsection{Context}

Paper~XIII identified H1 as the key
remaining hypothesis for completeness
$\Kspace=L^2(\mathbb{R})$.
Paper~XIV reduces H1 to a spectral
distribution problem for the
PSWF eigenbasis in the transition zone.
All conditional steps are explicitly labelled.

\subsection{H1: the norm-convergence problem}

\begin{hypothesis}[H1]
\label{hyp:H1}
For each $c_0>0$ and $g\in\PW_{c_0}^2$:
$\norm{a^{(c)}-a^{(\infty)}}_{\ell^2}\to 0$
($c\to\infty$).
\end{hypothesis}

H1 splits into two components:
\begin{align}
  &\mathrm{Re}\ip{a^{(c)}}{a^{(\infty)}}_{\ell^2}
    \to\norm{a^{(\infty)}}^2
    \tag{H1-ip}\label{eq:H1ip}\\
  &\norm{a^{(c)}}^2_{\ell^2}
    \to\norm{a^{(\infty)}}^2_{\ell^2}.
    \tag{H1-norm}\label{eq:H1norm}
\end{align}
\eqref{eq:H1ip} is proved unconditionally
(Prop.~\ref{prop:H1ip}).
The obstruction is entirely in~\eqref{eq:H1norm},
which is equivalent to H3
(given OP~\ref{prob:H1H3}).
The core difficulty:
$a^{(c)}\rightharpoonup a^{(\infty)}$
does not imply norm convergence;
Paper~XIV localises this gap precisely
as a spectral distribution problem.

\subsection{The central new framing}

\begin{definition}[Spectral measures and tail functional]
\label{def:spectral-measures}
For $n\ge 0$, $c>c_0$, define the
spectral probability measures
\[
  \mu_{n,c}(d\xi)
  :=|\widehat{\Phi_n^{(c)}}(\xi)|^2\,d\xi,
  \qquad
  \mu_{n,c}(\mathbb{R})=1\;(\text{Plancherel}).
\]
The tail functional is defined directly
via the PSWF-ONB expansion:
\begin{equation}\label{eq:SN-def}
  S_N(c) := \sum_{n>N}\mu_{n,c}([-c_0,c_0])
  = \sum_{n>N}\norm{\Pc\Phic{c}{n}}^2_{L^2}.
\end{equation}
This is a convergent series of non-negative
terms (each bounded by 1 by Plancherel);
no global operator-theoretic interpretation
is required for this definition.
The obstruction to H1 is
\emph{spectral non-evacuation}:
the failure of $S_N(c)$ to vanish
uniformly in $c$ as $N\to\infty$.
\end{definition}

\subsection{The transition zone}

\begin{definition}[Transition zone]
\label{def:transition}
For $0<\alpha<\kappa<1$:
$\mathcal{T}_{\alpha,\kappa}(c)
:=\{n: \alpha c < n \le \kappa c\}$.
This band has $\sim(\kappa-\alpha)c$ elements
and captures the regime where Slepian
eigenvalues $\lambda_n^{(c)}$
transit between $\approx 1$ and $\approx 0$.
\end{definition}

\subsection{Logical status}

\begin{center}
\fbox{\parbox{0.88\linewidth}{
\textbf{Proved unconditionally:}
\begin{itemize}[leftmargin=*]
\item $\norm{T_c}\le 1$
\item $S_N(c)$ defined via convergent PSWF-ONB sum
\item Tail reduction: $Q_c^N(g)\le\norm{g}^2 S_N(c)$
\item \eqref{eq:H1ip}
\item $\norm{\Bn}_{\mathrm{op}}\le 1$
  (composition of orthogonal projections)
\item $0\le\Ac\le I$
\item $\norm{\Ac}_{\mathrm{op}}\le S_N(c)$
  (quadratic-form + SOT + monotone majorisation;
  no global trace-class assumption)
\item $S_N(c)=\mathrm{Tr}_{\mathrm{PSWF}}(\Ac)$
  (limit of finite-dimensional traces)
\item $Q_c^N(g)=\ip{\Ac g}{g}$,
  $\Ac=\Bn^*\Bn\ge 0$
\item Finite-dimensional effective rank bound
  (Lemma~\ref{lem:rank-lower})
\item No single-mode dominance
  (Lemma~\ref{lem:no-single-mode},
  conditional on local Weyl law)
\item $\sigma(c)\sim cc_0/\pi$
  (classical Slepian--Weyl)
\item NC $\Rightarrow$ H3 fails
\item NC sufficient but not necessary
  (Prop.~\ref{prop:NC-not-H3})
\item Local Weyl law $\Rightarrow$ NC
\item H3 $\Rightarrow$ H1
\item H1 $\Rightarrow$ $\ip{\Ac g}{g}\to 0$
\item lim/sup non-interchangeable
  (Remark~\ref{rem:lim-sup})
\item Uniform (D-weak) $\Rightarrow$ (D-strong)
  (Remark~\ref{rem:double-scaling})
\item Diagonal reduction under (D-strong)
  (Prop.~\ref{prop:HS-reduction})
\item Near-diagonal HS sum $= O(1/c) = o(1)$
  under~(C) (Remark~\ref{rem:near-diagonal-structure})
\end{itemize}
\textbf{Open / conditional:}
\begin{itemize}[leftmargin=*]
\item $S_N^{(M(c))}(c)\gtrsim 1$,
  $M(c)\sim\beta c$
  (OP~\ref{prob:H1H3})
\item Local Weyl law~(C)
  (OP~\ref{prob:fourier-mass})
\item Intermediate-regime uniform
  transition estimates
  (OP~\ref{prob:intermediate}):
  \emph{primary analytic obstruction}
\end{itemize}}}
\end{center}

%%─── 2. INHERITED DATA
\section{Inherited Data}
\label{sec:input}

\begin{lemma}[From series]
\label{lem:inherited}
\begin{enumerate}[\rm(A)]
\item $\sum_n|a_n^{(c)}|^2\le\norm{g}^2$;
  $\norm{\Phic{c}{n}}_{L^2}=1$.
\item $a_n^{(c)}\to a_n^{(\infty)}$ (each fixed $n$).
\item For $n\le\kappa c$:
  $|\widehat{\Phi_n^{(c)}}(\xi)|\ge c_1 c^{-1/2}$
  on $E_c^{(n)}\subseteq[-c,c]$,
  $|E_c^{(n)}|\ge c_* c^{-1/2}$.
\item $\operatorname{supp}\hat g\subseteq[-c_0,c_0]$.
\end{enumerate}
\end{lemma}

%%═══ 3. CROSS-GRAM OPERATOR
\section{The Cross-Gram Operator}
\label{sec:crossgram}

\begin{definition}[Cross-Gram operator]
$T_c g:=\sum_{n\ge 0}\ip{g}{\Phic{c}{n}}\Phiinf{n}.$
\end{definition}

\begin{theorem}[$\norm{T_c}\le 1$]
\label{prop:Tc-bounded}
$\norm{T_c}\le 1$ uniformly in $c$.
\end{theorem}
\begin{proof}
Bessel:
$\norm{T_c g}^2=\sum_n|\ip{g}{\Phic{c}{n}}|^2
\le\norm{g}^2$.
\end{proof}

\begin{proposition}[\eqref{eq:H1ip}]
\label{prop:H1ip}
Condition~\eqref{eq:H1ip} holds unconditionally.
\end{proposition}
\begin{proof}
Paper~XII: $a^{(c)}\rightharpoonup a^{(\infty)}$
weakly in $\ell^2$.
\end{proof}

\begin{remark}
By weak lower semicontinuity:
$\liminf\norm{a^{(c)}}\ge\norm{a^{(\infty)}}$,
so H1 $\Leftrightarrow$
$\limsup_c\norm{a^{(c)}}\le\norm{a^{(\infty)}}$.
The entire obstruction is in the norm
upper bound~\eqref{eq:H1norm}.
\end{remark}

%%═══ 4. TAIL REDUCTION AND THE QUADRATIC FORM
\section{Tail Reduction, Operator Structure, and the Quadratic Form}
\label{sec:tail}

\begin{remark}[Foundational conventions]
\label{rem:foundation}
Throughout this section:
\begin{itemize}[leftmargin=*]
\item $S_N(c)$ is \emph{defined} by the
  convergent series~\eqref{eq:SN-def};
  not via a global operator trace.
\item $P_{c_0}$ is \emph{not}
  Hilbert--Schmidt on $L^2(\mathbb{R})$
  ($\|P_{c_0}\|_{\mathrm{HS}}^2=\infty$);
  no such property is used anywhere below.
\item The inequality
  $\norm{\Ac}_{\mathrm{op}}\le S_N(c)$
  is derived via the quadratic form of
  the monotone SOT-limit, not via
  convergence of operator norms.
  Eigenvalue-counting arguments are
  carried out \emph{only} at finite
  truncation level and are \emph{never}
  passed to the limit.
\end{itemize}
\end{remark}

\begin{definition}[$S_N(c)$, $\sigma(c)$,
  finite truncations]
\label{def:SN}
\begin{align*}
  S_N(c) &:= \sum_{n>N}\mu_{n,c}([-c_0,c_0]),\\
  S_N^{(M)}(c) &:= \sum_{n=N+1}^{M}
            \mu_{n,c}([-c_0,c_0]),
            \qquad M>N,\\
  \sigma(c) &:= \sum_{n\ge 0}\mu_{n,c}([-c_0,c_0])
            \sim \frac{cc_0}{\pi}
            \quad\text{(Slepian--Weyl
            \cite{Slepian1961})}.
\end{align*}
$S_N^{(M)}(c)\nearrow S_N(c)$ monotonely.
The divergence $\sigma(c)\to\infty$
does not preclude $S_N(c)\to 0$:
total spectral mass can grow while the
tail $\{n>N\}$ evacuates.
\end{definition}

\begin{lemma}[Tail reduction]
\label{lem:tail}
For $g\in\PW_{c_0}^2$:
\[
  Q_c^N(g):=\sum_{n>N}|a_n^{(c)}|^2
  \le\norm{g}^2\cdot S_N(c).
\]
\end{lemma}
\begin{proof}
Since $\operatorname{supp}\hat g\subseteq[-c_0,c_0]$,
Cauchy--Schwarz gives
$|a_n^{(c)}|^2
\le\norm{g}^2\cdot\mu_{n,c}([-c_0,c_0])$.
Sum over $n>N$.
\end{proof}

\begin{corollary}[Equi-tightness]
$\sup_c S_N(c)\to 0
\Rightarrow\{a^{(c)}\}_c$
equi-tight in $\ell^2$.
\end{corollary}

\begin{theorem}[Operator structure and
  spectral sum identity]
\label{thm:SN-operator}
Define:
\begin{align*}
  \Bn &:= (I-\Pi_N^{(c)})\Pc,\quad
  \Ac := \Bn^*\Bn,\\
  V_M^{(c)} &:= \mathrm{span}
    \{\Phic{c}{n} : N < n \le M\},\\
  \Ac^{(M)} &:= P_{V_M^{(c)}}P_{c_0}P_{V_M^{(c)}}
    \;:\; V_M^{(c)}\to V_M^{(c)}.
\end{align*}

\textbf{(0) Contraction.}
$\norm{\Bn}_{\mathrm{op}}\le 1$ (composition
of orthogonal projections), so
$0\le\Ac=\Bn^*\Bn\le I$,
and likewise $0\le\Ac^{(M)}\le I$
with eigenvalues $\lambda_j^{(M)}\in[0,1]$.

\medskip
\textbf{(I) Spectral sum identity.}
At each finite $M$:
\begin{equation}\label{eq:trace-M}
  \mathrm{Tr}(\Ac^{(M)})
  = \sum_{j}\lambda_j^{(M)}
  = S_N^{(M)}(c).
\end{equation}
Taking $M\to\infty$:
\begin{equation}\label{eq:TrPSWF}
  \mathrm{Tr}_{\mathrm{PSWF}}(\Ac)
  := \lim_{M\to\infty}\mathrm{Tr}(\Ac^{(M)})
  = S_N(c).
\end{equation}
This is a limit of genuine finite-dimensional
traces; it is \emph{not} a trace in
$\mathfrak{S}_1(L^2(\mathbb{R}))$,
nor a sum of eigenvalues of the global $\Ac$.

\medskip
\textbf{(II) Operator norm inequality.}
Since $P_{V_M^{(c)}}\to P_{V^{(c)}}$
strongly as $M\to\infty$,
we have $\Ac^{(M)}\to\Ac$ in the strong
operator topology (SOT).
For each $f\in L^2(\mathbb{R})$:
\begin{align}
  \ip{\Ac f}{f}
  &= \lim_{M\to\infty}
     \ip{\Ac^{(M)} f}{f}
  \nonumber\\
  &\le \lim_{M\to\infty}
     \norm{\Ac^{(M)}}_{\mathrm{op}}
     \norm{f}^2
  \nonumber\\
  &\le \lim_{M\to\infty}
     S_N^{(M)}(c)\cdot\norm{f}^2
  = S_N(c)\cdot\norm{f}^2.
  \label{eq:qf-bound}
\end{align}
The first equality uses SOT-convergence
($\Ac^{(M)}f\to\Ac f$ in norm,
hence inner products converge);
the second inequality uses the
finite-dimensional bound
$\norm{\Ac^{(M)}}_{\mathrm{op}}
=\max_j\lambda_j^{(M)}
\le\mathrm{Tr}(\Ac^{(M)})=S_N^{(M)}(c)$;
the final equality uses
$S_N^{(M)}(c)\nearrow S_N(c)$.
Taking $\sup_{\norm{f}=1}$:
\begin{equation}\label{eq:op-le-spectral}
  \norm{\Ac}_{\mathrm{op}}
  \le S_N(c).
\end{equation}
The argument is entirely via quadratic
forms and SOT-monotone majorisation;
no norm-convergence of $\Ac^{(M)}$
and no trace-class identification
of $\Ac$ is used or claimed.

\medskip
\textbf{(III) Quadratic form.}
\begin{equation}\label{eq:Qc-key}
  Q_c^N(g)
  = \sum_{n>N}|\ip{g}{\Phic{c}{n}}|^2
  = \norm{\Bn g}^2
  = \ip{\Ac g}{g}.
\end{equation}

\medskip
\textbf{(IV) The full hierarchy.}
\begin{equation}\label{eq:chain}
  \underbrace{\text{H3}}_{S_N(c)\to 0}
  \;\Rightarrow\;
  \norm{\Ac}_{\mathrm{op}}\to 0
  \;\Rightarrow\;
  \text{H1}
  \;\Rightarrow\;
  \ip{\Ac g}{g}\to 0\;(\text{each fixed }g).
\end{equation}

\medskip
\begin{center}
\fbox{\parbox{0.84\linewidth}{
\textbf{Key distinction.}
The obstruction is not the size of the
spectral mass $S_N(c)$, but the
\emph{distribution of the spectral mass
across eigenvalues} of $\Ac^{(M)}$.
A large trace is compatible with all mass
concentrated on a single eigenvalue;
controlling the number of large eigenvalues
requires spectral distribution input
beyond the trace alone.
This reflects that the relevant quantity is
not density per mode, but the
\emph{accumulation of spectral mass across
a linearly growing family of modes}
(see \S\ref{sec:szego}
and OP~\ref{prob:H1H3}).}}
\end{center}
\end{theorem}

\begin{remark}[Non-interchangeability of
  limit and supremum]
\label{rem:lim-sup}
The failure to upgrade
$\ip{\Ac g}{g}\to 0$ (each fixed $g$)
to $\norm{\Ac}_{\mathrm{op}}\to 0$
stems from the non-interchangeability of
$\lim_{c\to\infty}$ and $\sup_{\norm{g}=1}$:
\[
  \norm{\Ac}_{\mathrm{op}}
  = \sup_{\norm{g}=1}\ip{\Ac g}{g},
\]
and pointwise convergence over the unit ball
does not imply uniform convergence.
A sequence of positive operators can satisfy
$\ip{A_c g}{g}\to 0$ for each fixed $g$
while $\norm{A_c}_{\mathrm{op}}$ remains
bounded away from zero
(e.g.\ rank-one operators $A_c=e_c\otimes e_c$
with $e_c\rightharpoonup 0$).
\end{remark}

\begin{remark}[Convergence hierarchy]
\label{rem:convergence-hierarchy}
The chain~\eqref{eq:chain} distinguishes:
\begin{enumerate}[\rm(1)]
\item \emph{H3} ($S_N(c)\to 0$):
  controls all spectral mass simultaneously.
\item \emph{$\norm{\Ac}_{\mathrm{op}}\to 0$}:
  controls the largest eigenvalue uniformly.
\item \emph{$\ip{\Ac g}{g}\to 0$ each fixed $g$}
  (quadratic-form convergence): weakest;
  proved from H1.
\end{enumerate}
The gap between (3) and (2) is the content
of OP~\ref{prob:H1H3}, identified in
\S\ref{sec:szego} as a double-scaling
Szeg\H{o} problem
(Definition~\ref{def:double-scaling});
its resolution requires
collective quasi-orthogonality~(D-strong),
not merely pointwise decay~(D-weak)
(\S\ref{sec:PaperXV}).
\end{remark}

\begin{remark}[Spectral sum vs.\
  spectral distribution]
\label{rem:HS-trace}
\begin{equation}\label{eq:op-SN-relation}
  \norm{\Ac}_{\mathrm{op}} \le S_N(c),
  \qquad
  S_N^{(M)}(c)
  \le \norm{\Ac^{(M)}}_{\mathrm{op}}\cdot(M-N).
\end{equation}
$S_N(c)$ controls the spectral sum.
It does \emph{not} control how many
eigenvalues are large, nor the total
spectral mass $S_N^{(M(c))}(c)$
at growing scales: both require
spectral distribution input
(OP~\ref{prob:H1H3}).
\end{remark}

\begin{lemma}[Finite-dimensional effective
  rank bound]
\label{lem:rank-lower}
For every $\varepsilon\in(0,1)$ and
finite $M>N$:
\begin{equation}\label{eq:eff-rank-M}
  \#\{j:\lambda_j^{(M)}\ge\varepsilon\}
  \;\ge\;
  \frac{S_N^{(M)}(c)-\varepsilon(M-N)}{1-\varepsilon}.
\end{equation}
This bound is finite linear algebra at
each stage $M$ and is never passed to
the limit.
\end{lemma}

\begin{proof}
Split the trace at $\varepsilon$:
$S_N^{(M)}(c)
\le\#\{j:\lambda_j^{(M)}\ge\varepsilon\}
+\varepsilon(M-N)$.
Rearrange.
\end{proof}

%%═══ 5. SPECTRAL MEASURES AND NC
\section{Spectral Measures and No-Concentration}
\label{sec:NC}

\begin{definition}[No-Concentration (NC)]
\label{def:NC}
Fix $0<\alpha<\kappa<1$.
NC holds if $\exists\, c_*>0$ such that
for all large $c$:
\[
  \sum_{n\in\mathcal{T}_{\alpha,\kappa}(c)}
  \mu_{n,c}([-c_0,c_0]) \ge c_*>0.
\]
\end{definition}

\begin{remark}[NC and semiclassical equidistribution]
\label{rem:NC-QE}
Under the local Weyl law,
$\mu_{n,c}([-c_0,c_0])\sim c_0/c$
for $n\in\mathcal{T}_{\alpha,\kappa}(c)$,
giving band sum $\sim c_0(\kappa-\alpha)>0$.
This exhibits QE-type behaviour in the
sense of semiclassical equidistribution
\cite{Zworski2012}: each mode contributes
a share of frequency mass proportional to
the relative bandwidth $c_0/c$, consistent
with uniform phase-space spreading along
the energy surface $\{n/c\to\alpha\}$.
NC holds with $c_*=c_0(\kappa-\alpha)/2$.
A sub-proportional band of width $c^\beta$
($\beta<1$) gives sum
$\sim c_0 c^{\beta-1}\to 0$:
insufficient for NC.
\end{remark}

\begin{proposition}[Spectral Leakage Barrier]
\label{prop:barrier}
Under NC: for each fixed $N$,
$\liminf_{c\to\infty}S_N(c)\ge c_*>0$,
so H3 fails.
\end{proposition}
\begin{proof}
For large $c$: $\alpha c>N$,
so $\mathcal{T}_{\alpha,\kappa}(c)
\subseteq\{n>N\}$
and $S_N(c)\ge c_*$.
\end{proof}

\begin{proposition}[NC$^c$ does not imply H3;
  logical separation]
\label{prop:NC-not-H3}
\emph{This is a logical separation result
for abstract probability measure families
$\{\mu_{n,c}\}$.
The model below does not arise from
$|\widehat{\Phi_n^{(c)}}|^2\,d\xi$;
PSWF realizability is neither asserted
nor needed.
The model concentrates mass in a
sub-proportional window of width
$\sim\sqrt{c}$, incompatible with
semiclassical equidistribution, confirming
the abstract character of the separation.}

\medskip\noindent
There exists a family with
$\mu_{n,c}\ge 0$, $\mu_{n,c}(\mathbb{R})=1$,
such that NC fails yet H3 fails:
$\liminf_{c\to\infty}S_N(c)\ge 1$
for every fixed $N$.
In particular, NC is sufficient but not
necessary for H3 to fail.
\end{proposition}

\begin{proof}
Set $I_c:=\{n:|n-\lfloor\alpha c\rfloor|
\le\lfloor\sqrt{c}\rfloor\}$,
$\mu_{n,c}([-c_0,c_0]):=1/|I_c|$
for $n\in I_c$, $0$ otherwise.
For large $c$, $I_c\subseteq\{n>N\}$
so $S_N(c)\ge 1$.
NC fails because $I_c$ lies in a
sub-proportional window
escaping every fixed proportional band.
\end{proof}

%%═══ 6. H3 AND THE MAIN REDUCTION
\section{H3 and the Main Reduction}
\label{sec:H3}

\begin{hypothesis}[H3]
\label{hyp:H3}
$\sup_{c>c_0}S_N(c)\xrightarrow{N\to\infty}0$.
\end{hypothesis}

\begin{theorem}[H3 $\Rightarrow$ H1]
\label{thm:H3-H1}
H3 implies H1.
\end{theorem}
\begin{proof}
Fix $\varepsilon>0$, $g\in\PW_{c_0}^2$.
H3: choose $N$ with
$\sup_c S_N(c)<\varepsilon/(2\norm{g}^2)$.
Lemma~\ref{lem:tail}: $Q_c^N(g)<\varepsilon/2$
for all $c$.
Paper~XII: choose $c_\varepsilon$ with
$\sum_{n\le N}|a_n^{(c)}-a_n^{(\infty)}|^2
<\varepsilon/2$ for $c>c_\varepsilon$.
Adding gives H1.
\end{proof}

\begin{theorem}[H1 $\Rightarrow$
  quadratic-form convergence]
\label{thm:H1-H3-ptwise}
Assume H1. For each fixed $g$ and $N$:
$Q_c^N(g)=\ip{\Ac g}{g}\to 0$.
This is quadratic-form convergence;
it does not imply
$\norm{\Ac}_{\mathrm{op}}\to 0$
(Remark~\ref{rem:lim-sup}).
\end{theorem}
\begin{proof}
$Q_c^N(g)
\le 2\norm{a^{(c)}-a^{(\infty)}}^2_{\ell^2}
+2\sum_{n>N}|a_n^{(\infty)}|^2\to 0$.
\end{proof}

\begin{theorem}[Main reduction]
\label{thm:reduction}
\textup{(i)} Proved:
H3$\Rightarrow$H1,
H1$\Rightarrow\ip{\Ac g}{g}\to 0$,
NC$\Rightarrow$H3 fails,
local Weyl$\Rightarrow$NC,
NC sufficient not necessary.

\textup{(ii)} Open:
$S_N^{(M(c))}(c)\gtrsim 1$
for $M(c)\sim\beta c$
(OP~\ref{prob:H1H3});
local Weyl law~(C)
(OP~\ref{prob:fourier-mass});
intermediate-regime uniform estimates
(OP~\ref{prob:intermediate}).

\textup{(iii)} Conditional:
local Weyl$\Rightarrow$NC
$\Rightarrow$H3 fails$\Rightarrow$H1 fails.
\end{theorem}

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{lll}
\toprule
Implication & Status & Note \\
\midrule
H3$\Rightarrow$H1
  & \textbf{Proved} & --- \\
H1$\Rightarrow\ip{\Ac g}{g}\to 0$
  & \textbf{Proved} & quadratic-form conv. \\
H1$\Rightarrow\norm{\Ac}_{\mathrm{op}}\to 0$
  & Open & OP~\ref{prob:H1H3} \\
lim/sup non-interchangeable
  & \textbf{Proved} & Rem.~\ref{rem:lim-sup} \\
$0\le\Ac\le I$
  & \textbf{Proved} & projections compose \\
$\norm{\Ac}_{\mathrm{op}}\le S_N(c)$
  & \textbf{Proved}
  & QF + SOT + monotone majorisation \\
$\Ac^{(M)}=P_{V_M}P_{c_0}P_{V_M}$
  & \textbf{Identified}
  & Thm.~\ref{thm:SN-operator} \\
Fin.-dim.\ eff.\ rank bound
  & \textbf{Proved}
  & Lem.~\ref{lem:rank-lower};
    level $M$ only \\
No single-mode dominance
  & \textbf{Identified}
  & Lem.~\ref{lem:no-single-mode};
    cond.\ Weyl \\
$S_N^{(M(c))}(c)\gtrsim 1$,
  $M(c)\sim\beta c$
  & Open
  & OP~\ref{prob:H1H3} \\
NC$\Rightarrow$H3 fails
  & \textbf{Proved} & --- \\
NC sufficient, not necessary
  & \textbf{Proved} & abstract \\
Local Weyl$\Rightarrow$NC
  & \textbf{Proved} & --- \\
NC holds
  & Open
  & OP~\ref{prob:fourier-mass} \\
Near-diagonal HS sum $=o(1)$ under (C)
  & \textbf{Proved}
  & Rem.~\ref{rem:near-diagonal-structure} \\
Intermediate regime $=o(1)$
  & Open
  & OP~\ref{prob:intermediate};
    \emph{primary obstruction} \\
Uniform (D-weak)$\Rightarrow$(D-strong)
  & \textbf{Proved}
  & Rem.~\ref{rem:double-scaling} \\
Diagonal reduction under (D-strong)
  & \textbf{Proved}
  & Prop.~\ref{prop:HS-reduction} \\
\bottomrule
\end{tabular}
\end{center}

%%═══ 7. INTERPRETATION AND CONTEXT
\section{Interpretation and Context}
\label{sec:interp}

\begin{remark*}[Standing assumptions
  for \S\ref{sec:interp}]
\label{rem:standing}
All WKB-based and entrywise asymptotic
estimates in this section are:
\begin{enumerate}[\rm(i)]
\item conditional on the local Weyl
  law~\textup{(C)}
  (OP~\ref{prob:fourier-mass}),
  which fixes the normalisation scale
  $\mu_{n,c}([-c_0,c_0])\sim c_0/c$;
\item understood in a formal
  semiclassical sense;
\item uniform in
  $n,m\in\mathcal{T}_{\alpha,\kappa}(c)$,
  conditional on the amplitude bounds
  of OP~\ref{prob:fourier-mass}.
\end{enumerate}
These conditions are not repeated
at each occurrence below.
\end{remark*}

\subsection{The true obstruction:
  spectral non-evacuation}

\[
  \text{H1 fails}
  \;\Longleftrightarrow\;
  \text{spectral non-evacuation
  in }n\sim\alpha c
  \quad(\text{cond.\ on
  OPs~\ref{prob:H1H3},
  \ref{prob:fourier-mass}}).
\]
The model of
Proposition~\ref{prop:NC-not-H3}
shows mass can persist in $S_N(c)$
without any single proportional band
carrying it.

\subsection{Two levels of the problem}

\begin{center}
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{lll}
\toprule
Level & Quantity & Status \\
\midrule
Energetic
  & $S_N(c)$ (spectral sum)
  & Controlled \\
Geometric
  & $\#\{j:\lambda_j^{(M)}\ge\varepsilon\}$
  & Open \\
\bottomrule
\end{tabular}
\end{center}

\noindent
This is the exact distinction between
trace control and spectral distribution
control.
The open problem lives entirely at
the geometric level.

\subsection{Why existing tools are
  insufficient}

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{lll}
\toprule
Tool & Controls & Does not control \\
\midrule
Bessel
  & $\sum_{n\le N}\mu_{n,c}\le N+1$
  & Tail; NC \\
Paper~XII
  & quadratic-form conv.
  & $\norm{\Ac}_{\mathrm{op}}$;
    spectral distribution \\
Paper~VIII
  & $|\widehat{\Phi}|$ pointwise
  & $\mu_{n,c}([-c_0,c_0])$
    integrated \\
Slepian--Weyl
  & $\sigma(c)\sim cc_0/\pi$
  & Band distribution \\
$\mu_{n,c}$ equidistribution
  & NC; near-diagonal HS sum
  & Intermediate HS sum \\
(D-weak) uniform
  & $\sup_{n\ne m}
    |(\Ac^{(M)})_{nm}|=o(1/c)$
  & cumulative HS mass \\
\bottomrule
\end{tabular}
\end{center}

\subsection{Connection to semiclassical
  Szeg\H{o} theory}
\label{sec:szego}

\subsubsection*{The concrete operator form}

\begin{equation}\label{eq:AcM-concrete}
  \Ac^{(M)}
  = P_{V_M^{(c)}}P_{c_0}P_{V_M^{(c)}}
  \;:\; V_M^{(c)}\to V_M^{(c)},
\end{equation}
\begin{equation}\label{eq:gram}
  (\Ac^{(M)})_{nm}
  = \int_{-c_0}^{c_0}
    \widehat{\Phi_n^{(c)}}(\xi)\,
    \overline{\widehat{\Phi_m^{(c)}}(\xi)}
    \,d\xi,
  \quad N < n,m \le M.
\end{equation}

\subsubsection*{The double-scaling
  Szeg\H{o} regime}

\begin{definition}[Double-scaling
  Szeg\H{o} regime]
\label{def:double-scaling}
The family $\Ac^{(M(c))}$ constitutes
a \emph{double-scaling Szeg\H{o}
problem} if:
\begin{itemize}[leftmargin=*]
\item $c\to\infty$,
  $M(c)\sim\beta c$,
  fixed $\beta>0$;
\item $h:=1/c\to 0$,
  symbol $\mathbf{1}_{|\xi|\le c_0}$
  on $\{n/c\to\alpha\}$;
\item $\dim V_{M(c)}^{(c)}\sim\beta c
  \sim 1/h$.
\end{itemize}
This differs from classical
Toeplitz/Szeg\H{o} theory in that
the eigenbasis, the dimension, and
the semiclassical parameter all vary
simultaneously; the double limit
$\lim_c\lim_M$ and the joint limit
$\lim_{c,\,M\sim\beta c}$
do not commute.
\end{definition}

\begin{remark}[Uniform (D-weak)
  implies (D-strong)]
\label{rem:double-scaling}
If $|(\Ac^{(M)})_{nm}|
\le\varepsilon(c)/c$ uniformly over
all $O(c^2)$ off-diagonal pairs, then
$\|\Ac^{(M)}-D^{(M)}\|_{\mathrm{HS}}^2
\le\varepsilon(c)^2\to 0$.
The operative sufficient condition
for OP~\ref{prob:H1H3} is the weaker
collective condition (D-strong);
(D-weak) is not required.
\end{remark}

\begin{remark}[WKB reduction of the
  off-diagonal structure]
\label{rem:WKB-reduction}
\emph{No result in Paper~XIV depends
on the following; it serves only to
motivate the regime decomposition
and the structure of the open problems.}

\medskip\noindent
At WKB level:
\begin{equation}\label{eq:WKB-heuristic}
  \widehat{\Phi_n^{(c)}}(\xi)
  \;\approx_{\mathrm{WKB}}\;
  A_n(\xi)\,e^{i n \theta(\xi)/c},
  \qquad \xi\in[-c_0,c_0],
\end{equation}
giving Gram entries
\begin{equation}\label{eq:WKB-gram}
  (\Ac^{(M)})_{nm}
  \;\approx_{\mathrm{WKB}}\;
  \int_{-c_0}^{c_0}
  A_n(\xi)\,\overline{A_m(\xi)}\,
  e^{i(n-m)\theta(\xi)/c}\,d\xi.
\end{equation}
The regime decomposition follows from
the size of the phase
$(n-m)\theta(\xi)/c$:
oscillatory for $|n-m|\gg 1$,
$O(1/c)$ for $|n-m|=O(1)$,
transitional for
$C<|n-m|\le c^{\theta_0}$.
\end{remark}

\begin{remark}[Regime decomposition]
\label{rem:regime-decomposition}
Fix $0<\theta_0<1$ independently
of $c$; no uniformity in
$\theta_0\to 1^-$ is claimed.
The decomposition
\begin{equation}\label{eq:HS-regimes}
  \|\Ac^{(M)}-D^{(M)}\|_{\mathrm{HS}}^2
  = \Sigma_{\mathrm{near}}
  + \Sigma_{\mathrm{int}}
  + \Sigma_{\mathrm{far}}
\end{equation}
is a disjoint partition of the
off-diagonal index set
$\{(n,m): n\ne m,\,
n,m\in\mathcal{T}_{\alpha,\kappa}(c)
\cap(N,M]\}$
according to $|n-m|$.

\begin{itemize}[leftmargin=*]
\item \textbf{Near-diagonal
  ($|n-m|\le C$):}
  $O(c)$ pairs, entries $\Theta(1/c)$
  (Remark~\ref{rem:near-diagonal-structure}).
  $\Sigma_{\mathrm{near}}
  =O(c)\cdot O(1/c^2)=O(1/c)=o(1)$.
  This is a \emph{scaling regime},
  not a cancellation regime:
  control follows entirely from entry
  size and pair count, with no
  oscillatory input.
  Under~(C), this regime is
  \emph{subcritical in HS scaling}
  and is not a candidate for
  macroscopic spectral obstruction.

\item \textbf{Far regime
  ($|n-m|>c^{\theta_0}$):}
  Phase frequency
  $\ge c^{\theta_0-1}\to\infty$;
  van der Corput gives decay.
  $\Sigma_{\mathrm{far}}=o(1)$
  accessible via standard stationary
  phase methods.

\item \textbf{Intermediate regime
  ($C<|n-m|\le c^{\theta_0}$):}
  \emph{Sole genuine obstruction.}
  Under~(C): $O(c^{1+\theta_0})$
  pairs, entries $O(1/c)$,
  crude bound
  $\Sigma_{\mathrm{int}}
  =O(c^{\theta_0-1})=o(1)$
  for each fixed $\theta_0<1$.
  The bound is sufficient for each
  fixed $\theta_0$;
  the obstruction is structural, not
  quantitative --- it arises from the
  instability of the transition kernel
  under summation as $|n-m|/c\to 0$,
  reflecting the non-commutativity of
  the limit $c\to\infty$ and summation
  over a window of size
  $O(c^{\theta_0})$.
  Standard pointwise estimates degrade
  as $|n-m|/c\to 0$ and cannot be
  summed uniformly without new input
  (OP~\ref{prob:intermediate}).
\end{itemize}
\end{remark}

\begin{remark}[Near-diagonal structure]
\label{rem:near-diagonal-structure}
At WKB level, for $0<|k|\le C$:
\begin{equation}\label{eq:near-diag-amplitude}
  A_n\overline{A_{n+k}}
  = |A_n|^2
  + A_n\overline{(A_{n+k}-A_n)}.
\end{equation}
The correction term is $O(1/c)$
(smooth $n$-dependence of $A_n$
plus normalisation from~(C);
rigorous justification via Lipschitz
regularity of $n\mapsto A_n$ in
$L^2([-c_0,c_0])$ is part of
OP~\ref{prob:fourier-mass}).
Expanding the phase:
\begin{equation}\label{eq:near-diag-expansion}
  (\Ac^{(M)})_{n,n+k}
  \approx_{\mathrm{WKB}}
  \frac{c_0}{c}
  + \frac{ik}{c}
    \int_{-c_0}^{c_0}
    |A_n|^2\theta\,d\xi
  + O(c^{-2}).
\end{equation}
Entries are generically $\Theta(1/c)$,
not $o(1/c)$.
The collective HS sum:
\[
  \Sigma_{\mathrm{near}}
  = O(1)\cdot O(c)\cdot O(1/c^2)
  = O(1/c) = o(1).
\]
This confirms the regime is
\emph{subcritical in HS scaling}:
control is automatic under~(C)
and no cancellation mechanism
is required or invoked.
\end{remark}

\subsubsection*{No single-mode dominance}

\begin{lemma}[No single-mode dominance]
\label{lem:no-single-mode}
Under~(C), $M(c)\sim\beta c$:
$\max_{N<n\le M(c)}(\Ac^{(M)})_{nn}
=O(1/c)$.
Total mass $S_N^{(M(c))}\gtrsim 1$
requires collective contribution
of $\sim c$ modes.
\end{lemma}
\begin{proof}
Immediate from
$(\Ac^{(M)})_{nn}
=\mu_{n,c}([-c_0,c_0])\sim c_0/c$.
\end{proof}

\subsubsection*{Quasi-orthogonality
  and reduction}

\noindent\textbf{(D-weak).}
\begin{equation}\label{eq:quasi-orth}
  \sup_{n\ne m}
  \left|(\Ac^{(M)})_{nm}\right|
  \le \frac{\varepsilon(c)}{c},
  \quad\varepsilon(c)\to 0.
\end{equation}

\noindent\textbf{(D-strong).}
\begin{equation}\label{eq:HS-diag}
  \|\Ac^{(M)}-D^{(M)}\|_{\mathrm{HS}}
  \to 0,
  \quad M(c)\sim\beta c.
\end{equation}
(D-weak) implies (D-strong)
(Remark~\ref{rem:double-scaling})
but is strictly stronger since
near-diagonal entries are $\Theta(1/c)$.
The operative condition is (D-strong).

\begin{proposition}[Diagonal reduction]
\label{prop:HS-reduction}
Under~(C) and~(D-strong),
$M(c)\sim\beta c$:
\[
  S_N^{(M(c))}(c)\sim c_0\beta > 0.
\]
\end{proposition}
\begin{proof}
$S_N^{(M(c))}
=\beta c\cdot(c_0/c+o(1/c))
=c_0\beta+o(1)$.
\end{proof}

\begin{conjecture}[Double-scaling
  Szeg\H{o}]
\label{conj:szego}
Under~(C) and~(D-strong),
$M(c)\sim\beta c$:
\textup{(i)}~$\bar\lambda^{(M(c))}
\sim c_0/c\to 0$;
\textup{(ii)}~$S_N^{(M(c))}
\sim c_0\beta>0$;
\textup{(iii)}~$\#\{j:
\lambda_j^{(M(c))}\ge\varepsilon\}
\to\infty$;
\textup{(iv)}~$\|\Ac^{(M(c))}
-D^{(M(c))}\|_{\mathrm{HS}}\to 0$.
\end{conjecture}

\begin{remark}[Programme status]
\label{rem:programme-status}
(C)+(D-strong)
$\Rightarrow$~(ii)
[Prop.~\ref{prop:HS-reduction}]
$\Rightarrow$~(iii)
[Lem.~\ref{lem:rank-lower}]
$\Rightarrow$ OP~\ref{prob:H1H3}
closed.
Remaining open step:
$\Sigma_{\mathrm{int}}=o(1)$
(OP~\ref{prob:intermediate}).
\end{remark}

\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{lll}
\toprule
Result & Status & Reference \\
\midrule
Double-scaling Szeg\H{o} structure
  & \textbf{Identified}
  & Def.~\ref{def:double-scaling} \\
No single-mode dominance
  & \textbf{Identified}
  & Lem.~\ref{lem:no-single-mode} \\
Regime decomposition (disjoint)
  & \textbf{Identified}
  & Rem.~\ref{rem:regime-decomposition} \\
Near-diagonal = subcritical HS
  & \textbf{Identified}
  & Rem.~\ref{rem:near-diagonal-structure} \\
$\Sigma_{\mathrm{near}}=O(1/c)=o(1)$
  & \textbf{Proved} (cond.\ (C))
  & Rem.~\ref{rem:near-diagonal-structure} \\
$\Sigma_{\mathrm{far}}=o(1)$
  & Expected (stationary phase)
  & Rem.~\ref{rem:regime-decomposition} \\
$\Sigma_{\mathrm{int}}=o(1)$
  & \textbf{Open; sole obstruction}
  & OP~\ref{prob:intermediate} \\
(D-weak)$\Rightarrow$(D-strong)
  & \textbf{Proved}
  & Rem.~\ref{rem:double-scaling} \\
Diagonal reduction
  & \textbf{Proved}
  & Prop.~\ref{prop:HS-reduction} \\
$S_N^{(M(c))}\sim c_0\beta$
  & Conjectured
  & Conj.~\ref{conj:szego} \\
$S_N^{(M(c))}\gtrsim 1$
  & Open
  & OP~\ref{prob:H1H3} \\
NC holds
  & Open
  & OP~\ref{prob:fourier-mass} \\
\bottomrule
\end{tabular}
\end{center}

%%═══ 8. PROGRAMME FOR PAPER XV
\section{Programme for Paper~XV}
\label{sec:PaperXV}

\subsection{Required estimates}

\begin{enumerate}[\rm(A)]
\item \textbf{Upper bound.}
  $\mu_{n,c}([-c_0,c_0])\le C/c$,
  uniformly in
  $n\in\mathcal{T}_{\alpha,\kappa}(c)$.
  Required for:
  Lem.~\ref{lem:no-single-mode},
  $\Sigma_{\mathrm{near}}=o(1)$.

\item \textbf{Lower bound.}
  $\mu_{n,c}([-c_0,c_0])\ge 1/(Cc)$,
  uniformly in
  $n\in\mathcal{T}_{\alpha,\kappa}(c)$.
  Required for: NC,
  OP~\ref{prob:fourier-mass}.

\item \textbf{Asymptotic~(C).}
  $\mu_{n,c}([-c_0,c_0])
  = c_0/c + o(1/c)$,
  uniformly in
  $n\in\mathcal{T}_{\alpha,\kappa}(c)$.
  Required for:
  Prop.~\ref{prop:HS-reduction},
  Conj.~\ref{conj:szego},
  $\Sigma_{\mathrm{near}}=o(1)$.

\item[\rm(D-int)]
  \textbf{Intermediate-regime
  transition estimate.}
  Here $\theta_0\in(0,1)$ is fixed
  independently of $c$; no uniformity
  in $\theta_0\to 1^-$ is claimed.
  Establish
  $\Sigma_{\mathrm{int}}=o(1)$
  for $M(c)\sim\beta c$
  (OP~\ref{prob:intermediate}).
  Under~(C): $O(c^{1+\theta_0})$
  pairs, entries $O(1/c)$, giving
  crude bound
  $O(c^{\theta_0-1})=o(1)$
  for each fixed $\theta_0$.
  This bound is sufficient but
  non-uniform in $\theta_0$;
  a uniform transition estimate
  as $|n-m|/c\to 0$ requires
  new input beyond~(C).

\item[\rm(D-far)]
  \textbf{Far-regime estimate.}
  $\Sigma_{\mathrm{far}}=o(1)$
  via double-scaling stationary phase
  for $|n-m|>c^{\theta_0}$.
  Expected accessible by standard
  semiclassical methods.
\end{enumerate}

\subsection{Regime strategy}

The three regimes require separate
treatment:

\begin{enumerate}[\rm(i)]
\item \textbf{Near-diagonal.}
  Subcritical in HS scaling under~(C);
  no further input needed.
  \emph{Structurally eliminated as
  obstruction candidate.}

\item \textbf{Far regime.}
  Phase oscillates with frequency
  $\ge c^{\theta_0-1}\to\infty$;
  standard van der Corput or
  double-scaling stationary phase
  applies.
  Accessible by existing
  semiclassical methods.

\item \textbf{Intermediate regime.}
  \emph{Sole genuine obstruction.}
  Crude bound is sufficient per fixed
  $\theta_0$; the structural
  difficulty is the instability of
  the transition kernel under
  summation as $|n-m|/c\to 0$,
  reflecting non-commutativity of
  $c\to\infty$ and summation over
  $O(c^{\theta_0})$ pairs.
  Required tools:
  \begin{itemize}[leftmargin=*]
  \item uniform stationary phase
    with parameter
    $\delta:=|n-m|/c\in
    (C/c,\,c^{\theta_0-1})$;
  \item van der Corput uniform in
    $\delta\to 0$;
  \item WKB amplitude bounds uniform
    in $n\in\mathcal{T}_{\alpha,\kappa}(c)$
    (from OP~\ref{prob:fourier-mass});
  \item $L^2$ estimates on
    $n$-dependence of
    $\widehat{\Phi_n^{(c)}}$
    via the PSWF differential equation.
  \end{itemize}
\end{enumerate}

\subsection{Logical flow of Paper~XV}

\begin{center}
\fbox{\parbox{0.88\linewidth}{
\textbf{Primary target:}
(A)--(C)
$+$ $\Sigma_{\mathrm{int}}=o(1)$
$+$ $\Sigma_{\mathrm{far}}=o(1)$
$\Rightarrow$ (D-strong)
$\Rightarrow$
Prop.~\ref{prop:HS-reduction}
$\Rightarrow$ OP~\ref{prob:H1H3}
closed
$\Rightarrow$ H3 fails
$\Rightarrow$ (cond.\ (C)) H1 fails.

\medskip\noindent
$\Sigma_{\mathrm{near}}=o(1)$
already established under~(C);
requires no further input.

\medskip\noindent
All other regimes are either
automatically controlled or
amenable to standard semiclassical
methods; the entire obstruction
is localised in the intermediate
scale $C<|n-m|\le c^{\theta_0}$.

\medskip\noindent
\textbf{Independent route:}
(A)$+$(B)
$\Rightarrow$ OP~\ref{prob:fourier-mass}
$\Rightarrow$ NC
$\Rightarrow$ H3 fails,
without (D-strong) or
Conj.~\ref{conj:szego}.}}
\end{center}

%%═══ 9. OPEN PROBLEMS
\section{Open Problems}
\label{sec:open}

\begin{problem}[Local Weyl law;
  Paper~XV]
\label{prob:fourier-mass}
\emph{Priority: critical.}

Prove estimates~(A)--(C):
\[
  \mu_{n,c}([-c_0,c_0])
  = \frac{c_0}{c}
  + o\!\left(\frac{1}{c}\right),
\]
uniformly in
$n\in\mathcal{T}_{\alpha,\kappa}(c)$.

\medskip\noindent
\textbf{Approach.}
WKB/semiclassical analysis of
$\widehat{\Phi_n^{(c)}}(\xi)$
in the transition zone, uniform
in $n\sim\alpha c$.
The main difficulty is uniformity
as $n/c\to\alpha$, where the
classical turning point approaches
the boundary of $[-c,c]$.
\cite{Osipov2013} provides the
single-mode framework;
the uniform version requires
additional treatment at the
turning point.

\medskip\noindent
\textbf{Role.}
(A) closes OP~\ref{prob:fourier-mass}
and yields NC.
(C) is required for
Prop.~\ref{prop:HS-reduction},
Conj.~\ref{conj:szego}, and
$\Sigma_{\mathrm{near}}=o(1)$.
\end{problem}

\begin{problem}[Total spectral mass
  at linear scales]
\label{prob:H1H3}
\emph{Priority: high.}

Show: for some $\beta>0$,
\[
  \liminf_{c\to\infty}
  S_N^{(M(c))}(c) > 0,
  \quad M(c)\sim\beta c.
\]

\medskip\noindent
\textbf{Reduction.}
By Prop.~\ref{prop:HS-reduction}:
follows from (C)$+$(D-strong).
(D-strong) decomposes as
$\Sigma_{\mathrm{near}}
+\Sigma_{\mathrm{int}}
+\Sigma_{\mathrm{far}}=o(1)$.
$\Sigma_{\mathrm{near}}=o(1)$:
proved.
$\Sigma_{\mathrm{far}}=o(1)$:
expected accessible.
Sole remaining step:
$\Sigma_{\mathrm{int}}=o(1)$
(OP~\ref{prob:intermediate}).

\medskip\noindent
\textbf{Role.}
Confirms H3 fails, hence
(conditionally) H1 fails,
establishing that the Slepian
eigenbasis does not provide
coefficient stability for generic
$g\in\PW_{c_0}^2$.
\end{problem}

\begin{problem}[Intermediate-regime
  uniform transition estimate]
\label{prob:intermediate}
\emph{Primary analytic target;
  Paper~XV.}

Fix $\theta_0\in(0,1)$ independently
of $c$. Establish:
\begin{equation}\label{eq:intermediate}
  \Sigma_{\mathrm{int}}
  = \sum_{\substack{n\ne m \\
    C<|n-m|\le c^{\theta_0}}}
  \left|(\Ac^{(M)})_{nm}\right|^2
  = o(1),
  \quad M(c)\sim\beta c.
\end{equation}

\medskip\noindent
\textbf{Why this is hard.}
The crude bound
$O(c^{\theta_0-1})=o(1)$
is sufficient for each fixed $\theta_0$
but non-uniform as $\theta_0\to 1^-$.
The obstruction is structural, not
quantitative: it arises from the
instability of the transition kernel
under summation as $|n-m|/c\to 0$,
reflecting non-commutativity of
the limit $c\to\infty$ and summation
over a window of $O(c^{\theta_0})$
pairs.
In contrast, the near-diagonal regime
has only $O(c)$ pairs and is
subcritical in HS scaling;
the intermediate regime has
$c^{\theta_0}$ times more pairs and
requires uniform transition estimates
not accessible via~(C) alone.

\medskip\noindent
\textbf{Approach.}
Uniform stationary phase with
parameter $\delta:=|n-m|/c\to 0$;
van der Corput uniform in $\delta$;
WKB amplitude bounds from
OP~\ref{prob:fourier-mass};
$L^2$ estimates via the PSWF
differential equation.

\medskip\noindent
\textbf{Role.}
Together with
$\Sigma_{\mathrm{near}}=o(1)$
(proved) and
$\Sigma_{\mathrm{far}}=o(1)$
(accessible),
yields (D-strong) and closes
OP~\ref{prob:H1H3} via
Prop.~\ref{prop:HS-reduction}.
\end{problem}

\begin{problem}[NC from Paper~VIII]
\label{prob:NC-direct}
\emph{Priority: medium.}

Do the pointwise bounds of Paper~VIII
directly imply
$\mu_{n,c}([-c_0,c_0])\ge\delta/c$
without full WKB asymptotics?
A positive answer gives NC via a
shorter route independent of the
WKB programme.
\end{problem}

%%═══ 10. SUMMARY
\section{Summary}
\label{sec:summary}

\begin{center}
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{lll}
\toprule
Result & Status & Reference \\
\midrule
$\norm{T_c}\le 1$
  & \textbf{Proved}
  & Thm.~\ref{prop:Tc-bounded} \\
$S_N(c)$ via convergent series
  & \textbf{Proved}
  & Def.~\ref{def:spectral-measures} \\
Tail reduction
  & \textbf{Proved}
  & Lem.~\ref{lem:tail} \\
\eqref{eq:H1ip}
  & \textbf{Proved}
  & Prop.~\ref{prop:H1ip} \\
$0\le\Ac\le I$
  & \textbf{Proved}
  & Thm.~\ref{thm:SN-operator} \\
$\norm{\Ac}_{\mathrm{op}}\le S_N(c)$
  & \textbf{Proved}
  & Thm.~\ref{thm:SN-operator} \\
$S_N(c)=\mathrm{Tr}_{\mathrm{PSWF}}(\Ac)$
  & \textbf{Proved}
  & Thm.~\ref{thm:SN-operator} \\
H3$\Rightarrow$H1
  & \textbf{Proved}
  & Thm.~\ref{thm:H3-H1} \\
NC$\Rightarrow$H3 fails
  & \textbf{Proved}
  & Prop.~\ref{prop:barrier} \\
NC sufficient, not necessary
  & \textbf{Proved}
  & Prop.~\ref{prop:NC-not-H3} \\
(D-weak)$\Rightarrow$(D-strong)
  & \textbf{Proved}
  & Rem.~\ref{rem:double-scaling} \\
Diagonal reduction
  & \textbf{Proved}
  & Prop.~\ref{prop:HS-reduction} \\
$\Sigma_{\mathrm{near}}=O(1/c)=o(1)$
  & \textbf{Proved} (cond.\ (C))
  & Rem.~\ref{rem:near-diagonal-structure} \\
Double-scaling Szeg\H{o} structure
  & \textbf{Identified}
  & Def.~\ref{def:double-scaling} \\
Near-diagonal = subcritical HS
  & \textbf{Identified}
  & Rem.~\ref{rem:near-diagonal-structure} \\
Intermediate = sole obstruction
  & \textbf{Identified}
  & Rem.~\ref{rem:regime-decomposition} \\
$\Sigma_{\mathrm{far}}=o(1)$
  & Expected
  & stationary phase \\
$S_N^{(M(c))}\sim c_0\beta$
  & Conjectured
  & Conj.~\ref{conj:szego} \\
Local Weyl law~(C)
  & \textbf{Open}
  & OP~\ref{prob:fourier-mass} \\
$\Sigma_{\mathrm{int}}=o(1)$
  & \textbf{Open; primary target}
  & OP~\ref{prob:intermediate} \\
$S_N^{(M(c))}\gtrsim 1$
  & \textbf{Open}
  & OP~\ref{prob:H1H3} \\
\bottomrule
\end{tabular}
\end{center}

\medskip\noindent
\textbf{Central conclusion.}
\emph{The obstruction to coefficient
stability is spectral non-evacuation
in the transition zone $n\sim\alpha c$.
All operator-theoretic structure is
established without global trace-class
or Hilbert--Schmidt assumptions,
via the concrete compression
$\Ac^{(M)}=P_{V_M^{(c)}}P_{c_0}
P_{V_M^{(c)}}$, SOT-convergence,
and quadratic-form monotone majorisation.
The gap between quadratic-form
convergence (proved from H1) and
spectral distribution control is
identified as a double-scaling
Szeg\H{o} problem.
Its resolution requires collective
quasi-orthogonality~(D-strong),
which decomposes into three regimes:
the near-diagonal regime is
subcritical in HS scaling and
automatically controlled under~(C);
the far regime is amenable to
standard stationary phase; and
the entire obstruction is genuinely
localised in the intermediate scale
$C<|n-m|\le c^{\theta_0}$, where the
structural difficulty is the
instability of the transition kernel
under summation as $|n-m|/c\to 0$,
reflecting the non-commutativity of
the limit $c\to\infty$ and summation
over a growing window of pairs.
No single mode carries macroscopic
mass; mass accumulates collectively
across $\sim c$ modes.
Paper~XIV is logically complete up
to three open problems, each
precisely localised and analytically
tractable.}

\begin{thebibliography}{99}

\bibitem{PaperXIV-XIII}
[Author], \textit{Paper~XIII}, 2026.

\bibitem{PaperXIV-XII}
[Author], \textit{Paper~XII}, 2026.

\bibitem{PaperXIV-VIII}
[Author], \textit{Paper~VIII}, 2026.

\bibitem{Slepian1961}
D.~Slepian, H.~O.~Pollak,
\textit{Prolate spheroidal wave
functions, Fourier analysis and
uncertainty~I},
Bell Syst.\ Tech.\ J.\
\textbf{40} (1961), 43--63.

\bibitem{Osipov2013}
A.~Osipov, V.~Rokhlin, H.~Xiao,
\textit{Prolate Spheroidal Wave
Functions of Order Zero},
Springer, 2013.

\bibitem{ReedSimon2}
M.~Reed, B.~Simon,
\textit{Methods of Modern Mathematical
Physics, Vol.~II},
Academic Press, 1975.

\bibitem{Conway1990}
J.~B.~Conway,
\textit{A Course in Functional
Analysis}, 2nd ed., Springer, 1990.

\bibitem{Zworski2012}
M.~Zworski,
\textit{Semiclassical Analysis},
AMS, 2012.

\bibitem{BottcherSilbermann1990}
A.~B\"ottcher, B.~Silbermann,
\textit{Analysis of Toeplitz
Operators}, Springer, 1990.

\bibitem{Simon2011}
B.~Simon,
\textit{Szeg\H{o}'s Theorem and Its
Descendants},
Princeton University Press, 2011.

\end{thebibliography}

\end{document}
