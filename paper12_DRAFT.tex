\documentclass[12pt,a4paper]{amsart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{booktabs}

%%%─── Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{conjecture}[theorem]{Conjecture}
\newtheorem{hypothesis}[theorem]{Hypothesis}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{problem}[theorem]{Open Problem}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}
\newtheorem*{remark*}{Remark}

%%%─── Macros
\newcommand{\norm}[1]{\|#1\|}
\newcommand{\ip}[2]{\langle #1,\, #2\rangle}
\newcommand{\Hlim}{\mathcal{H}_{\lim}}
\newcommand{\Lc}{\mathcal{L}_c}
\newcommand{\Pninf}{P_n^{(\infty)}}
\newcommand{\Pnck}{P_n^{(c_k)}}
\newcommand{\spc}[1]{\sigma(#1)}

\subjclass[2020]{47A10, 47B25, 42C05, 46B15, 34E20}
\keywords{Prolate spheroidal wave functions, spectral projection stability,
approximate eigenvector, strong operator convergence,
spectral gap, limiting operator, Riesz basis}

\begin{document}

\title[A Localization Principle for Spectral Projection Stability]{%
  A Localization Principle for Spectral Projection Stability
  under Strong Operator Convergence}

\author{[Author]}
\address{[Address]}
\email{[Email]}

\begin{abstract}
\textbf{Main result.}
For bounded self-adjoint operators with
$A_k\xrightarrow{\mathrm{SOT}} A$,
under an explicit spectral isolation
assumption (Assumption~S),
the gap-normalized residual condition
\[
  \frac{\|(A_k-A)\phi\|}{\gamma_k}
  +\frac{|\lambda_k-\lambda|}{\gamma_k}\to 0
\]
is sufficient for rank-one spectral
projection stability
$\|P_k - P^\infty\|\to 0$,
and necessary in the bounded-gap regime
$\gamma_k\ge c_0>0$.
This is a \emph{localization principle}
--- by which we mean: dependence only on
the residual evaluated on a single
eigenvector, normalized by the local
spectral gap ---
that goes strictly beyond
Davis--Kahan/Kato-type global
operator-norm control
(Lemma~\ref{lem:dk-comparison}).

\medskip
We further establish:
a No-Go result (Prop.~\ref{prop:no-go})
showing eigenvalue density alone does not
control spectral angles;
a counterexample (Prop.~\ref{prop:counterexample})
confirming sharpness when gap control fails;
and a reduction of PSWF projection stability
to two independent analytic mechanisms
(Gap-S and SOT-Faster).
\end{abstract}

\maketitle

%%══ 1. INTRODUCTION
\section{Introduction}
\label{sec:intro}

\subsection{Context and goal}

\begin{center}
\fbox{\parbox{0.88\linewidth}{\centering\itshape
SOT produces approximate eigenvectors,
but does not control the residual relative to the spectral gap.\\
This is the precise obstruction to spectral projection stability.}}
\end{center}

\medskip
\noindent
\textbf{Main Principle (informal).}
Let $A_k\xrightarrow{\mathrm{SOT}} A$
with isolated simple eigenvalue $\lambda$
of $A$ and associated simple eigenvalues
$\lambda_k$ of $A_k$
(Assumption~S below).
Then:
\begin{equation}\label{eq:main-principle-informal}
  \frac{\|(A_k-A)\phi\|}{\gamma_k}
  +\frac{|\lambda_k-\lambda|}{\gamma_k}
  \to 0
  \;\Longrightarrow\;
  \|P_k-P^\infty\|\to 0.
\end{equation}
The converse holds when $\gamma_k\ge c_0>0$
(Thm.~\ref{thm:necessary-bounded-gap}).
See Thm.~\ref{thm:abstract} for the
precise statement.

\medskip
Paper~X establishes $\mathcal{H}_{c_k}
\xrightarrow{\mathrm{SOT}}\Hlim$;
Paper~XI constructs an orthonormal diagonal
eigenfunction system $\{\Phi_n^{(\infty)}\}$.
This paper proves:
\begin{itemize}
\item \eqref{eq:main-principle-informal}
  is the \emph{sharp sufficient mechanism}
  (Thm.~\ref{thm:abstract}).
\item When $\gamma_k\ge c_0>0$,
  it is also \emph{necessary}
  (Thm.~\ref{thm:necessary-bounded-gap}).
\item The condition strictly extends
  Davis--Kahan: if $\|A_k-A\|\to 0$
  then \eqref{eq:main-principle-informal}
  holds automatically
  (Lem.~\ref{lem:dk-comparison}).
\item Eigenvalue density alone does not
  control spectral angles
  (Prop.~\ref{prop:no-go}).
\item SOT $+$ eigenvalue convergence
  is insufficient without gap control
  (Prop.~\ref{prop:counterexample}).
\item PSWF projection stability reduces
  to Gap-S and SOT-Faster
  (Thms.~\ref{thm:convergence},~\ref{thm:rate}).
\end{itemize}

\medskip
\noindent
\textit{Projection stability under strong operator
convergence is not governed by global
perturbation size, but by a purely local
spectral-geometric quantity:
the residual relative to the spectral gap.}

\begin{remark}[Scope]
\label{rem:scope}
Thms.~\ref{thm:abstract},
\ref{thm:necessary-bounded-gap}
and Props.~\ref{prop:no-go},
\ref{prop:counterexample} are fully rigorous.
All PSWF content is encoded in
Hyps.~\ref{hyp:gap-structural}
and~\ref{hyp:sot-faster}.
\end{remark}

\subsection{What this paper does \emph{not} claim}
\begin{itemize}
\item Full iff when $\gamma_k\to 0$:
  Open Problem~\ref{prob:full-iff}.
\item Proof of Gap-S or SOT-Faster:
  Open Problems~\ref{prob:sharp-gc},
  \ref{prob:sot-faster}.
\item Completeness $\mathcal{K}=L^2$:
  Paper~XIII.
\item Spectral isolation without
  Assumption~S: see
  Rem.~\ref{rem:spectral-isolation}.
\end{itemize}

\subsection{Role in the series}
\begin{center}
\begin{tabular}{lll}
\toprule
Paper & Content & Status\\
\midrule
X   & SOT: $\mathcal{H}_c
  \xrightarrow{\mathrm{SOT}}\Hlim$ & Final\\
XI  & Diagonal system
  $\{\Phi_n^{(\infty)},\lambda_n^{(\infty)}\}$
  & Final\\
\textbf{XII} & \textbf{Localization principle,
  No-Go, Counterexample,
  PSWF reduction}
  & \textbf{This paper}\\
XIII& Completeness $\mathcal{K}=L^2$
  & Planned\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{Guide for the Referee}
\begin{itemize}
\item Spectral isolation axiom:
  Assumption~S (Sec.~\ref{sec:abstract}).
\item Davis--Kahan comparison:
  Lem.~\ref{lem:dk-comparison}.
\item Abstract Stability Principle
  (sufficient): Thm.~\ref{thm:abstract}.
\item Rank-one projection $\Rightarrow$
  eigenvector convergence:
  Lem.~\ref{lem:rank-one-proj}.
\item Necessary mechanism
  (bounded gap): Thm.~\ref{thm:necessary-bounded-gap}.
\item Novelty discussion:
  Rem.~\ref{rem:abstract-novelty}.
\item No-Go and counterexample:
  Props.~\ref{prop:no-go},~\ref{prop:counterexample}.
\item PSWF reduction:
  Thms.~\ref{thm:convergence},~\ref{thm:rate}.
\item Open problems:
  Sec.~\ref{sec:open}.
\end{itemize}

\tableofcontents

%%══ 2. INPUT
\section{Input from Papers VIII, X, XI}
\label{sec:input}

\begin{lemma}[Inherited data]
\label{lem:inherited}
\begin{enumerate}[\rm(A)]
\item\label{A1}
  \emph{(VIII, Cor.~4.3)}
  $|\lambda_n^{(c)}-\lambda_n^{(\infty)}|
  \le C_\kappa c^{-1/4}$,
  $n\le\kappa c$, uniform in $n$.
\item\label{A2}
  \emph{(X, Thread~A)}
  $\mathcal{H}_{c_k}\xrightarrow{\mathrm{SOT}}\Hlim$:
  \begin{equation}\label{eq:sot}
    \|(\mathcal{H}_{c_k}-\Hlim)f\|\to 0
    \quad\forall\, f\in L^2.
  \end{equation}
  No rate on \eqref{eq:sot}.
\item\label{A3}
  \emph{(XI, Lem.~3.1 + Thm.~5.1)}
  $c_k\nearrow\infty$,
  $\ip{\Phi_m^{(\infty)}}{\Phi_n^{(\infty)}}=\delta_{mn}$,
  $\Hlim\Phi_n^{(\infty)}=\lambda_n^{(\infty)}\Phi_n^{(\infty)}$,
  $\lambda_n^{(c)}$ simple for all $n$, $c$.
\end{enumerate}
\end{lemma}

\begin{lemma}[Eigenvalue matching]
\label{lem:eigenvalue-matching}
$|\lambda_n^{(c_k)}-\lambda_n^{(\infty)}|
\le C_\kappa c_k^{-1/4}$.
\end{lemma}
\begin{proof}Direct from \eqref{A1}.\end{proof}

\begin{remark}[SOT $\not\Rightarrow$
  projection norm convergence]
\label{rem:strong-vs-norm}
\cite[Ex.~VIII.7.14]{ReedSimon1975}:
SOT does not imply norm convergence
of spectral projections in general.
\end{remark}

%%══ 3. HYPOTHESES AND NO-GO
\section{Hypotheses, Structural Necessity,
  and No-Go}
\label{sec:gap}

\begin{hypothesis}[Effective gap, Gap-S]
\label{hyp:gap-structural}
Let $W_c:=\{n:\lambda_n^{(c)}
\in(\delta,1-\delta)\}$ for fixed $\delta>0$.
There exists $g\colon[1,\infty)\to(0,\infty)$
such that
\begin{equation}\label{eq:gap-dominates}
  \frac{c^{-1/4}}{g(c)}\to 0
  \quad\text{as }c\to\infty,
\end{equation}
and for all $c\ge 1$,
uniformly in $n\in W_c$:
\begin{equation}\label{eq:gap-s}
  |\lambda_n^{(c)}-\lambda_{n+1}^{(c)}|
  \ge g(c).
\end{equation}
\end{hypothesis}

\begin{remark}[Gap-S: necessity within SOT arguments;
  determinantal context]
\label{rem:gap-necessary}
Gap-S is necessary for any argument
based solely on local spectral gap control
and SOT-type information,
as demonstrated by Prop.~\ref{prop:no-go}:
without \eqref{eq:gap-dominates},
the ratio $\tilde\varepsilon_n(k)/g(c_k)$
in \eqref{eq:ratio-to-zero} cannot be shown
to vanish.
A different proof strategy might encode
an equivalent condition differently,
but must capture the same gap-relative
scale information.

\textbf{Density vs.\ gap.}
Widom~\cite{Widom1964} controls average
density ($\sim(\log c)^{-1}$ spacing),
not the minimal gap.
Average spacing $\ne$ minimum gap;
Gap-S is strictly stronger
(Prop.~\ref{prop:no-go}).

\textbf{Determinantal analogy.}
In determinantal point processes,
minimal gaps are controlled via
sine-kernel repulsion.
Gap-S is the analogous deterministic
bound for the PSWF kernel;
Open Problem~\ref{prob:sharp-gc}.
\end{remark}

\begin{remark}[$c^{-1/4}/g(c)\to 0$]
\label{rem:gap-implies-o}
Condition \eqref{eq:gap-dominates}
states explicitly that $c^{-1/4}/g(c)\to 0$,
i.e.\ the eigenvalue-matching error $C_\kappa c^{-1/4}$
is of strictly smaller order than the gap $g(c)$.
This ratio is used in \eqref{eq:ratio-to-zero}.
\end{remark}

The following result shows that any approach
based solely on eigenvalue density must fail,
even under SOT convergence and eigenvalue
convergence.

\begin{proposition}[No-Go: eigenvalue
  density does not control spectral angles]
\label{prop:no-go}
Let $(\mu_n)$ be a sequence with
$\liminf_n(\mu_n-\mu_{n+1})=0$
but with Widom-type average density.
Then there exists a sequence
$B_k\xrightarrow{\mathrm{SOT}} B$
of bounded self-adjoint operators
with eigenvalues $\mu_n^{(k)}\to\mu_n$
such that $\|Q_n^{(k)}-Q_n\|\not\to 0$
for infinitely many $n$.
\end{proposition}
\begin{proof}
Choose indices $n_j$ along which
$\gamma_j := \mu_{n_j} - \mu_{n_j+1} \to 0$.
For each $j$, define on
$\mathrm{span}\{e_{n_j}, e_{n_j+1}\}$
the symmetric $2\times 2$ matrix
\[
  B_k^{(j)} :=
  \begin{pmatrix}
    \mu_{n_j}^{(k)} & \varepsilon_j(k) \\
    \varepsilon_j(k) & \mu_{n_j+1}^{(k)}
  \end{pmatrix},
  \quad
  \varepsilon_j(k) := \sqrt{\gamma_j^{(k)}},
\]
where $\gamma_j^{(k)}
:= \mu_{n_j}^{(k)} - \mu_{n_j+1}^{(k)}
\to \gamma_j \to 0$.
Set $B_k := \bigoplus_j B_k^{(j)}$
(block-diagonal, uniformly bounded);
then $B_k \xrightarrow{\mathrm{SOT}} B$.

For the $j$-th block,
the eigenvector of $B_k^{(j)}$
corresponding to the upper eigenvalue
makes an angle $\theta_j^{(k)}$
with $e_{n_j}$ satisfying
\[
  \tan\bigl(2\theta_j^{(k)}\bigr)
  = \frac{2\varepsilon_j(k)}{\gamma_j^{(k)}}
  = \frac{2}{\sqrt{\gamma_j^{(k)}}}
  \to +\infty.
\]
Hence $\theta_j^{(k)} \to \pi/4$,
so the upper eigenvector tends to
$\tfrac{1}{\sqrt{2}}(e_{n_j}+e_{n_j+1})$
rather than $e_{n_j}$.
Therefore
$\|Q_{n_j}^{(k)} - Q_{n_j}\|
\ge |\ip{Q_{n_j}^{(k)}e_{n_j}}{e_{n_j}}
- 1|
= |\cos^2\theta_j^{(k)} - 1|
\to \tfrac{1}{2}$,
so projection convergence fails for all $j$.
\end{proof}

\begin{remark}
\begin{center}\itshape
The example shows that eigenvalue density
alone --- even of Widom type --- does not
suffice to control spectral projection
stability: it does not control whether
the normalized residual
$\|(A_k - A)\phi\|/\gamma_k$
vanishes, even under eigenvalue convergence
$\lambda_k\to\lambda$ and asymptotic
spacing of order $(\log c)^{-1}$.
Minimal-gap control relative to the
residual is therefore an additional
structural assumption, independent of
density information.
\end{center}
\end{remark}

\begin{hypothesis}[SOT-Faster]
\label{hyp:sot-faster}
\begin{equation}\label{eq:sot-faster}
  \varepsilon_n(k) :=
  \|(\mathcal{H}_{c_k}-\Hlim)
  \Phi_n^{(\infty)}\|
  = o(g(c_k)).
\end{equation}
\end{hypothesis}

\begin{remark}[SOT-Faster as microlocal
  norm convergence]
\label{rem:sot-faster-localized}
SOT-Faster is strictly stronger than
$\varepsilon_n(k)\to 0$ (SOT gives this freely).
It encodes norm control localised to a
spectral window of width $\sim g(c_k)$
around $\lambda_n^{(\infty)}$:
SOT-Faster follows from
$\|(\mathcal{H}_{c_k}-z)^{-1}
-(\Hlim-z)^{-1}\|\cdot g(c_k)\to 0$
for $|z-\lambda_n^{(\infty)}|\asymp g(c_k)$.
\end{remark}

\begin{hypothesis}[SOT-Rate]
\label{hyp:sot-rate}
$\varepsilon_n(k) = O(c_k^{-1/4})$.
\end{hypothesis}

\begin{remark}[Logical hierarchy]
\label{rem:hyp-hierarchy}
SOT-Rate $\Rightarrow$ SOT-Faster
$\Rightarrow$ $\|P_n^{(c_k)}-P_n^{(\infty)}\|\to 0$.
SOT alone gives $\varepsilon_n(k)\to 0$;
gap comparison is strictly additional.
\end{remark}

\begin{remark}[Plausibility of SOT-Rate]
\label{rem:sot-rate-plausible}
Expanding in eigenbasis $\{\Phi_m^{(c_k)}\}$:
\begin{equation}\label{eq:formal-expansion}
  \varepsilon_n(k)^2
  = \sum_m
  |\lambda_m^{(c_k)}-\lambda_n^{(\infty)}|^2
  |\ip{\Phi_n^{(\infty)}}{\Phi_m^{(c_k)}}|^2.
\end{equation}
Diagonal $m=n$: $\le C_\kappa^2 c_k^{-1/2}$.
Small off-diagonal overlaps
(consistent with Gap-S)
predict $\varepsilon_n(k)=O(c_k^{-1/4})$;
Prob.~\ref{prob:off-diagonal}.
\end{remark}

\subsection{WKB insufficiency}
\begin{theorem}[WKB insufficiency]
\label{thm:wkb-indeterminacy}
WKB/Airy analysis of
$\Lc=-\tfrac{d}{dx}[(1-x^2)
\tfrac{d}{dx}]+c^2x^2$
cannot determine the scaling of
$|\lambda_{n+1}^{(c)}-\lambda_n^{(c)}|$
without global kernel information.
\end{theorem}
\begin{proof}
Sigmoid profile of $F_c$~\cite{Widom1964};
any algebraic rate is a global claim.
\end{proof}

%%══ 4. ABSTRACT STABILITY PRINCIPLE
\section{Abstract Stability Principle}
\label{sec:abstract}

\begin{assumption}[Spectral isolation, Assumption~S]
\label{ass:spectral-isolation}
Let $\{A_k\}$ and $A$ be bounded
self-adjoint on $\mathcal{H}$ with
$A_k\xrightarrow{\mathrm{SOT}} A$.
We assume:
\begin{enumerate}[\rm(S1)]
\item\label{S1}
  $\lambda\in\sigma(A)$ is isolated and
  simple; $P^\infty$ is the rank-one
  projection onto $\ker(A-\lambda)$.
\item\label{S2}
  For each $k$, $\lambda_k\in\sigma(A_k)$
  is isolated and simple, with
  $\gamma_k := \mathrm{dist}(\lambda_k,
  \sigma(A_k)\setminus\{\lambda_k\})>0$
  and rank-one projection $P_k$.
\item\label{S3}
  $\lambda_k\to\lambda$.
\end{enumerate}
\end{assumption}

\begin{remark}[Spectral isolation under SOT]
\label{rem:spectral-isolation}
Under strong resolvent convergence
(SRC, i.e.\ $(A_k-z)^{-1}\to (A-z)^{-1}$
in norm for $z\notin\sigma(A)$),
Assumption~S follows automatically from
isolation of $\lambda$ via Riesz
projections~\cite[Ch.~VIII]{Kato1966}.
Under mere SOT, SRC may fail, so
Assumption~S must be imposed explicitly.
In the PSWF application, (S1)--(S3) are
supplied by Lem.~\ref{lem:inherited}(A3)
and Lem.~\ref{lem:eigenvalue-matching}.
\end{remark}

\begin{lemma}[Davis--Kahan implies ratio
  condition]
\label{lem:dk-comparison}
Under Assumption~S,
if $\|A_k - A\|\to 0$, then
\[
  \frac{\|(A_k-A)\phi\|}{\gamma_k}
  +\frac{|\lambda_k-\lambda|}{\gamma_k}
  \to 0
\]
provided $\gamma_k \ge c_0 > 0$.
In particular, the ratio condition
\eqref{eq:abstract-ratio} is strictly
weaker than $\|A_k-A\|\to 0$:
\[
  \|A_k-A\|\to 0
  \;\Longrightarrow\;
  \eqref{eq:abstract-ratio}
  \;\Longrightarrow\;
  \|P_k-P^\infty\|\to 0,
\]
but neither implication reverses in general
(Prop.~\ref{prop:counterexample}).
\end{lemma}
\begin{proof}
$\|(A_k-A)\phi\|\le\|A_k-A\|\cdot\|\phi\|=
\|A_k-A\|\to 0$.
Eigenvalue convergence:
$|\lambda_k-\lambda|\le\|A_k-A\|\to 0$
by spectral stability under norm convergence.
Dividing by $\gamma_k\ge c_0$:
both terms $\to 0$.
\end{proof}

\begin{theorem}[Main Principle;
  sufficient condition]
\label{thm:abstract}
Under Assumption~S,
if the ratio condition
\begin{equation}\label{eq:abstract-ratio}
  \frac{\|(A_k-A)\phi\|}{\gamma_k}
  +\frac{|\lambda_k-\lambda|}{\gamma_k}
  \to 0
\end{equation}
holds, then $\|P_k - P^\infty\|\to 0$.
\end{theorem}
\begin{proof}
Set $\varepsilon_k:=\|(A_k-A)\phi\|$,
$\delta_k:=|\lambda_k-\lambda|$.
By triangle inequality:
$\|(A_k-\lambda_k)\phi\|
\le\varepsilon_k+\delta_k$.
Lem.~\ref{lem:approx-eig} gives
$\|(I-P_k)\phi\|
\le(\varepsilon_k+\delta_k)/\gamma_k\to 0$.
Rank-one identity~\cite[Ch.~1]{Bhatia1997}
yields $\|P_k-P^\infty\|\to 0$.
\end{proof}

\begin{lemma}[Rank-one projection convergence
  implies eigenvector convergence]
\label{lem:rank-one-proj}
Let $P_k = v_k\otimes v_k$ and
$P^\infty = \phi\otimes\phi$
be rank-one orthogonal projections
with $\|v_k\|=\|\phi\|=1$.
If $\|P_k - P^\infty\|\to 0$, then
there exist phases $\theta_k\in\mathbb{R}$
such that
\[
  \|v_k - e^{i\theta_k}\phi\|\to 0.
\]
\end{lemma}
\begin{proof}
Since $\|P_k - P^\infty\|\to 0$,
we have for all $f\in\mathcal{H}$:
$\|P_k f - P^\infty f\|\to 0$.
Applying this to $f=\phi$
(and using $P^\infty\phi=\phi$):
\[
  \|v_k\ip{v_k}{\phi} - \phi\|\to 0.
\]
Set $\alpha_k:=\ip{v_k}{\phi}$.
Then $\|v_k\alpha_k - \phi\|\to 0$
implies $|\alpha_k|\to 1$
(since $\|v_k\alpha_k\|=|\alpha_k|$
and $\|\phi\|=1$).
Choose $\theta_k$ such that
$e^{i\theta_k}\alpha_k=|\alpha_k|>0$.
Then
\begin{align*}
  \|v_k - e^{i\theta_k}\phi\|
  &\le \|v_k\alpha_k - \phi\|/|\alpha_k|
  + \|e^{i\theta_k}\phi\|\cdot|1-|\alpha_k||/|\alpha_k|
  \to 0,
\end{align*}
since $|\alpha_k|\to 1$ and
$\|v_k\alpha_k-\phi\|\to 0$.
\end{proof}

\begin{theorem}[Necessary condition;
  bounded-gap case]
\label{thm:necessary-bounded-gap}
Under Assumption~S,
assume $\|P_k-P^\infty\|\to 0$
and $\gamma_k\ge c_0>0$.
Then \eqref{eq:abstract-ratio} holds.
\end{theorem}
\begin{proof}
The simplicity of $\lambda$ ensures
that $P^\infty$ is rank-one and that
the decompositions used below are
well-defined.

\textbf{Eigenvalue term.}
$|\lambda_k-\lambda|/\gamma_k
\le|\lambda_k-\lambda|/c_0\to 0$.

\textbf{Residual term.}
Decompose $\phi=P_k\phi+(I-P_k)\phi$:
\begin{equation}\label{eq:decomp-necessary}
  (A_k-A)\phi
  = (A_k-A)P_k\phi
  + (A_k-A)(I-P_k)\phi.
\end{equation}
\textit{Term~(II).}
$\|(A_k-A)(I-P_k)\phi\|
\le(\sup_k\|A_k\|+\|A\|)\|(I-P_k)\phi\|$.
$\sup_k\|A_k\|\le M<\infty$
by uniform boundedness.
$\|(I-P_k)\phi\|\to 0$ since
$P^\infty\phi=\phi$ and $\|P_k-P^\infty\|\to 0$.
Hence Term~(II) $\to 0$.

\textit{Term~(I).}
Write $P_k\phi=\alpha_k v_k$,
$\alpha_k:=\ip{v_k}{\phi}$,
$A_k v_k=\lambda_k v_k$; so
$(A_k-A)P_k\phi
= \alpha_k(\lambda_k v_k - Av_k)$.
Decompose:
$\lambda_k v_k - Av_k
= (\lambda_k-\lambda)v_k
+ (\lambda-A)v_k$.
First part:
$\|(\lambda_k-\lambda)v_k\|
=|\lambda_k-\lambda|\to 0$.
Second part: since $(A-\lambda)\phi=0$,
\[
  (A-\lambda)v_k
  = (A-\lambda)(v_k - e^{i\theta_k}\phi);
\]
hence
$\|(A-\lambda)v_k\|
\le\|A-\lambda\|\cdot\|v_k-e^{i\theta_k}\phi\|\to 0$
by Lem.~\ref{lem:rank-one-proj}.
Since $|\alpha_k|\le 1$:
Term~(I) $\to 0$.

Combining: $\|(A_k-A)\phi\|\to 0$;
dividing by $\gamma_k\ge c_0$ gives
\eqref{eq:abstract-ratio}.
\end{proof}

\begin{remark}[Uniform gap is essential for necessity]
\label{rem:necessity-uniform-gap}
The assumption $\gamma_k\ge c_0>0$
cannot be dropped in
Thm.~\ref{thm:necessary-bounded-gap}.
Without a uniform lower bound on $\gamma_k$,
the normalization by $\gamma_k$ becomes
unstable: the quantity
$\|(A_k-A)\phi\|/\gamma_k$ may diverge
even when projection convergence holds.
The vanishing-gap regime is encoded in
Hyp.~\ref{hyp:sot-faster}
and Open Problem~\ref{prob:full-iff}.
\end{remark}

\begin{remark}[Rank-one structure is essential]
\label{rem:rank-one-essential}
The necessity direction crucially uses
rank-one structure of $P_k$, $P^\infty$
to extract $\|v_k-e^{i\theta_k}\phi\|\to 0$
directly from $\|P_k-P^\infty\|\to 0$
(Lem.~\ref{lem:rank-one-proj}).
For higher-multiplicity eigenspaces,
the argument fails without additional
assumptions (e.g.\ reduced resolvent
convergence or angular subspace bounds).
\end{remark}

\begin{remark}[Open: full iff when
  $\gamma_k\to 0$]
\label{rem:iff-open}
When $\gamma_k\to 0$,
the proof above shows $\|(A_k-A)\phi\|\to 0$
but whether the rate is faster than
$\gamma_k$ is not determined by projection
convergence alone
(Hyp.~\ref{hyp:sot-faster},
Open Prob.~\ref{prob:full-iff}).
\end{remark}

\begin{remark}[Novelty: a one-vector
  perturbation principle]
\label{rem:abstract-novelty}
While residual-gap estimates are classical
(Chatelin~\cite{Chatelin1983},
Osborn~\cite{Osborn1975},
Kato~\cite{Kato1966} Ch.~IV),
their formulation as a purely one-vector
sufficient and (under bounded gap)
necessary condition under mere SOT
convergence appears not to be standard.

\textbf{Classical theory}
(Davis--Kahan~\cite{DavisKahan1970},
Kato~\cite{Kato1966}):
$\|P_k - P^\infty\| \lesssim
\|A_k - A\|/\mathrm{gap}$,
requiring $\|A_k-A\|\to 0$.
\textbf{Present result:}
no access to $\|A_k - A\|$;
global control may fail completely,
yet stability is governed by a single
\emph{vector-local} residual
$\|(A_k-A)\phi\|$ relative to the gap
(see implication diagram in
Lem.~\ref{lem:dk-comparison}).

By \emph{localization principle}
we mean: dependence only on the residual
evaluated on a single eigenvector $\phi$,
normalized by the local spectral gap.
We call this a \emph{one-vector
perturbation principle}.
\end{remark}

\begin{proposition}[Counterexample:
  SOT but projection failure]
\label{prop:counterexample}
There exist bounded self-adjoint $\{A_k\}$,
$A$ on $\ell^2$ satisfying Assumption~S
with $A_k\xrightarrow{\mathrm{SOT}} A$,
$\lambda_k\to\lambda$, yet
$\|P_k-P^\infty\|\not\to 0$.
\end{proposition}
\begin{proof}
Set $\phi=e_1$, $\lambda=0$,
$P^\infty=e_1\otimes e_1$,
$\gamma_k:=k^{-1/2}$,
$v_k:=\tfrac{1}{\sqrt{2}}(e_1+e_2)$,
\[
  A:=\sum_{n\ge 2} n^{-1}(e_n\otimes e_n),
  \quad
  A_k:=\gamma_k(v_k\otimes v_k)
  +\sum_{n\ge 2} n^{-1}(e_n\otimes e_n).
\]
(S1)--(S3) of Assumption~S hold:
$\lambda=0$ is isolated and simple for $A$;
$\lambda_k=\gamma_k$ is simple for $A_k$
with gap $\gamma_k'\sim\gamma_k$;
$\lambda_k\to 0$.

$\|(A_k-A)\phi\|=\gamma_k/2$, so
$\|(A_k-A)\phi\|/\gamma_k'\to 1/2\ne 0$:
ratio condition \eqref{eq:abstract-ratio}
fails.
Direct computation confirms
$\|P_k - P^\infty\|\not\to 0$,
consistent with Thm.~\ref{thm:necessary-bounded-gap}.
\end{proof}

\begin{remark}[Sharpness]
\label{rem:counterexample-meaning}
SOT $+$ eigenvalue convergence alone
do not suffice;
the ratio condition is tight.
\end{remark}

%%══ 5. PSWF APPLICATION
\section{PSWF Application: Main Theorems}
\label{sec:main}

This section reduces the PSWF projection
stability problem \emph{entirely} to two
independent analytic mechanisms:
gap control (Gap-S) and
localized SOT rate (SOT-Faster).
No further PSWF-specific input is needed
beyond Lem.~\ref{lem:inherited}.
The contribution of this section is a
\emph{complete reduction} of PSWF
projection stability to two independent
analytic problems, each of which can be
studied by separate methods
(Probs.~\ref{prob:sharp-gc},
\ref{prob:sot-faster}).
Assumption~S is verified by
Lem.~\ref{lem:inherited}(A3)
and Lem.~\ref{lem:eigenvalue-matching}.

Thms.~\ref{thm:convergence}
and~\ref{thm:rate} apply Thm.~\ref{thm:abstract}
with
$A_k=\mathcal{H}_{c_k}$, $A=\Hlim$,
$\phi=\Phi_n^{(\infty)}$,
$\lambda=\lambda_n^{(\infty)}$,
$\lambda_k=\lambda_n^{(c_k)}$.

\begin{remark}[Gap inequality in application]
\label{rem:gap-inequality}
Gap-S supplies the lower bound
$|\lambda_n^{(c_k)}-\lambda_{n\pm 1}^{(c_k)}|
\ge g(c_k)$, hence $\gamma_k\ge g(c_k)$.
Dividing by $g(c_k)$ suffices to verify
\eqref{eq:abstract-ratio}.
\end{remark}

\begin{theorem}[Projection convergence]
\label{thm:convergence}
Assume Lem.~\ref{lem:inherited},
Hyp.~\ref{hyp:gap-structural} (Gap-S),
and Hyp.~\ref{hyp:sot-faster} (SOT-Faster).
For each fixed $n\in W_{c_k}$:
\[
  \|P_n^{(c_k)} - P_n^{(\infty)}\|\to 0.
\]
In particular,
$\Phi_n^{(c_k)}\to\Phi_n^{(\infty)}$
strongly (up to phase)
and $\{\lambda_n^{(\infty)}\}$ has no
accumulation point in $(0,1)$
(Rem.~\ref{rem:nacc}).
\end{theorem}

\begin{theorem}[Rate]
\label{thm:rate}
Assume additionally Hyp.~\ref{hyp:sot-rate}.
For $n\le\kappa c_k$, $n\in W_{c_k}$,
uniformly in $n$:
\[
  \|P_n^{(c_k)} - P_n^{(\infty)}\|
  \le \frac{C\,C_\kappa\,c_k^{-1/4}}{g(c_k)}.
\]
If $g(c)\sim(\log c)^{-1}$:
rate $O(c_k^{-1/4}\log c_k)$.
\end{theorem}

\begin{remark}[Phase]
Phase fixed by
$\ip{\Phi_n^{(c_k)}}{\Phi_n^{(\infty)}}>0$
for large $k$.
\end{remark}

%%══ 6. PROOFS
\section{Proofs}
\label{sec:proj}

\begin{lemma}[Approx.\ eigenvector
  $\Rightarrow$ projection bound]
\label{lem:approx-eig}
Let $A$ bounded self-adjoint,
$\lambda$ isolated and simple,
$\gamma:=\mathrm{dist}(\lambda,
\spc{A}\setminus\{\lambda\})>0$,
$P$ rank-one onto $\ker(A-\lambda)$.
By the spectral theorem
(functional calculus),
the simplicity of $\lambda$ ensures
that $(A-\lambda)$ is invertible on
$\mathrm{ran}(I-P)$ with
$\|(A-\lambda)^{-1}|_{\mathrm{ran}(I-P)}\|
\le 1/\gamma$.
For $\|\phi\|=1$ and
$\|(A-\lambda)\phi\|\le\varepsilon$:
\[
  \|(I-P)\phi\|\le\varepsilon/\gamma.
\]
\end{lemma}
\begin{proof}
$(A-\lambda)\phi=(A-\lambda)(I-P)\phi$
since $(A-\lambda)P=0$.
By the spectral theorem, all spectral
values of $A$ on $\mathrm{ran}(I-P)$
lie at distance $\ge\gamma$ from $\lambda$;
hence $(A-\lambda)^{-1}$ exists on
$\mathrm{ran}(I-P)$ with norm $\le 1/\gamma$.
Therefore $\|(I-P)\phi\|\le\varepsilon/\gamma$.
\end{proof}

\begin{remark}[Rank-one structure]
\label{rem:perturbation-mechanism}
Rank-one gives uniform resolvent bound
$1/\gamma$ on $\mathrm{ran}(I-P)$.
Gap-S supplies $\gamma\ge g(c_k)$;
SOT-Faster gives
$\varepsilon_n(k)/g(c_k)\to 0$.
\end{remark}

\begin{lemma}[SOT: approx.\ eigenvectors,
  no rate]
\label{lem:sot-approx}
$\varepsilon_n(k)=
\|(\mathcal{H}_{c_k}-\Hlim)\Phi_n^{(\infty)}\|
\to 0$.
\end{lemma}
\begin{proof}SOT \eqref{eq:sot}.\end{proof}

\begin{proof}[Proof of Thm.~\ref{thm:convergence}]
Fix $n\in W_{c_k}$.
Assumption~S is verified by
Lem.~\ref{lem:inherited}(A3)
and Lem.~\ref{lem:eigenvalue-matching}.
\[
  \tilde\varepsilon_n(k)
  :=\|(\mathcal{H}_{c_k}-\lambda_n^{(c_k)})
  \Phi_n^{(\infty)}\|
  \le\varepsilon_n(k)+C_\kappa c_k^{-1/4}.
\]
By Rem.~\ref{rem:gap-inequality},
$\gamma_k\ge g(c_k)$; hence:
\begin{equation}\label{eq:ratio-to-zero}
  \frac{\tilde\varepsilon_n(k)}{g(c_k)}
  \le
  \underbrace{\frac{\varepsilon_n(k)}
    {g(c_k)}}_{\to 0\,(\text{SOT-Faster})}
  +\underbrace{\frac{C_\kappa c_k^{-1/4}}
    {g(c_k)}}_{\to 0
    \,(\text{Rem.}\,\ref{rem:gap-implies-o})}
  \to 0.
\end{equation}
Lem.~\ref{lem:approx-eig} with
$\gamma_k\ge g(c_k)$ gives
$\|(I-P_n^{(c_k)})\Phi_n^{(\infty)}\|\to 0$;
rank-one identity completes.
\end{proof}

\begin{proof}[Proof of Thm.~\ref{thm:rate}]
SOT-Rate: $\varepsilon_n(k)\le C'c_k^{-1/4}$;
$\tilde\varepsilon_n(k)\le(C'+C_\kappa)c_k^{-1/4}$;
Lem.~\ref{lem:approx-eig} with
$\gamma_k\ge g(c_k)$ gives the bound.
\end{proof}

\begin{remark}[No accumulation]
\label{rem:nacc}
Gap-S bounds finitely many
$\lambda_n^{(\infty)}$ in any interval.
\end{remark}

%%══ 7. RIESZ COROLLARY
\section{Riesz-Basis Corollary}
\label{sec:riesz-trivial}
\begin{remark}
Follows from orthonormality \eqref{A3};
not a contribution.
\end{remark}
\begin{corollary}[Riesz basis, trivial]
\label{cor:riesz}
$\{\Phi_n^{(\infty)}\}$ is a Riesz basis
of $\mathcal{K}$ with bounds $1,1$.
\end{corollary}

%%══ 8. CANONICALIZATION (OPTIONAL)
\section{Canonicalization (Optional)}
\label{sec:canonical}
\begin{remark}[Status]
Independent of main results.
\end{remark}
\begin{hypothesis}[Full family convergence]
\label{hyp:full-family}
Spectral measures of $\mathcal{H}_{c_k}$
converge weak-$*$ to those of $\Hlim$.
\end{hypothesis}
\begin{lemma}[Same $\mathcal{K}$, conditional]
\label{lem:same-K}
Under Hyp.~\ref{hyp:full-family},
any two Gap-S subsequences yield
the same $\mathcal{K}$.
\end{lemma}
\begin{proof}
Closed $\Hlim$-invariant subspaces;
Hyp.~\ref{hyp:full-family} identifies
spectral measures.
\end{proof}
\begin{theorem}[Canonicalization, conditional]
\label{thm:canonical}
Under Gap-S $+$ Hyp.~\ref{hyp:full-family},
$\Hlim$ is canonical on $\mathcal{K}$
up to unitary equivalence.
\end{theorem}

%%══ 9. STRUCTURAL EVIDENCE
\section{Structural Evidence for Gap-S
  and SOT-Faster}
\label{sec:heuristic}

\textbf{Non-rigorous throughout.}

\subsection{Sigmoid transition (Widom)}
Widom~\cite{Widom1964} predicts
$\lambda_n^{(c)}\approx
(1+e^{\pi(n-2c/\pi)/\sigma(c)})^{-1}$
with $\sigma(c)\asymp\log c$;
average spacing $\sim(\log c)^{-1}$ in $W_c$,
consistent with Gap-S.

\subsection{Density vs.\ minimum gap}
Widom controls density, not minimum gap.
Typical spacing $\gg c^{-1/4}$ supports
Gap-S for typical indices;
rigorous lower bound needs
determinantal methods
(Prob.~\ref{prob:sharp-gc}).

\subsection{SOT-Faster plausibility}
Small off-diagonal overlaps in
\eqref{eq:formal-expansion}
predict $\varepsilon_n(k)=O(c_k^{-1/4})$;
Prob.~\ref{prob:off-diagonal}.

\begin{remark}[Status]
Section~\ref{sec:heuristic} provides
structural evidence, not proof.
\end{remark}

%%══ 10. OPEN PROBLEMS
\section{Open Problems}
\label{sec:open}

\noindent\textbf{Conjecture-level:}

\begin{problem}[Sharp gap scale]
\label{prob:sharp-gc}
Prove Gap-S with $g(c)\sim(\log c)^{-1}$
via sine-kernel repulsion.
\end{problem}

\begin{problem}[SOT-Faster]
\label{prob:sot-faster}
Prove $\varepsilon_n(k)=o(g(c_k))$;
equivalently: local norm-resolvent bound
at scale $g(c_k)$.
\end{problem}

\begin{problem}[Full iff, vanishing gap]
\label{prob:full-iff}
Characterise when
$\|P_k-P^\infty\|\to 0$
$\Rightarrow$
$\|(A_k-A)\phi\|/\gamma_k\to 0$
for $\gamma_k\to 0$.
\end{problem}

\noindent\textbf{Technical-level:}

\begin{problem}[SOT-Rate]
\label{prob:sot-rate}
$\varepsilon_n(k)=O(c_k^{-1/4})$.
\end{problem}

\begin{problem}[Off-diagonal overlaps]
\label{prob:off-diagonal}
$|\ip{\Phi_n^{(\infty)}}{\Phi_m^{(c_k)}}|$
for $m\ne n$.
\end{problem}

\begin{problem}[Strong resolvent convergence]
\label{prob:src}
Does $\mathcal{H}_{c_k}$ converge
strongly in resolvent sense?
This would make Assumption~S
automatic for isolated eigenvalues.
\end{problem}

\begin{problem}[Full family convergence]
\label{prob:full-family}
Establish Hyp.~\ref{hyp:full-family}.
\end{problem}

\begin{problem}[Completeness]
\label{prob:completeness}
$\mathcal{K}=L^2(\mathbb{R})$? Paper~XIII.
\end{problem}

%%%─── BIBLIOGRAPHY
\begin{thebibliography}{99}
\bibitem{PaperXI}[Author],
\textit{Spectral structure for the
limiting PSWF operator}, Paper~XI, 2026.

\bibitem{PaperX}[Author],
\textit{Mosco form convergence and
the bridge theorem}, Paper~X, 2026.

\bibitem{PaperVIII}[Author],
\textit{Non-cancellation control and
the sharp $c^{-1/2}$ lower bound},
Paper~VIII, 2026.

\bibitem{Slepian1961}
D.~Slepian and H.~O.~Pollak,
\textit{Prolate spheroidal wave functions~I},
Bell Syst.\ Tech.\ J.\ \textbf{40}
(1961), 43--63.

\bibitem{Osipov2013}
A.~Osipov, V.~Rokhlin, H.~Xiao,
\textit{Prolate Spheroidal Wave Functions
of Order Zero}, Springer, 2013.

\bibitem{Widom1964}
H.~Widom,
\textit{Asymptotic behavior of eigenvalues~II},
Arch.\ Ration.\ Mech.\ Anal.\ \textbf{17}
(1964), 215--229.

\bibitem{DavisKahan1970}
C.~Davis and W.~M.~Kahan,
\textit{The rotation of eigenvectors~III},
SIAM J.\ Numer.\ Anal.\ \textbf{7}
(1970), 1--46.

\bibitem{Bhatia1997}
R.~Bhatia, \textit{Matrix Analysis},
Springer, 1997.

\bibitem{Kato1966}
T.~Kato,
\textit{Perturbation Theory for
Linear Operators}, Springer, 1966.

\bibitem{ReedSimon1975}
M.~Reed and B.~Simon,
\textit{Methods of Modern Mathematical
Physics}, Academic Press, 1972--1975.

\bibitem{GohbergKrein1969}
I.~Gohberg and M.~Krein,
\textit{Linear Nonselfadjoint Operators},
AMS, 1969.

\bibitem{DLMF2023}
F.~W.~J.~Olver et al.,
\textit{NIST DLMF}, Release 1.1.10, 2023.

\bibitem{Chatelin1983}
F.~Chatelin,
\textit{Spectral Approximation of
Linear Operators},
Academic Press, 1983.

\bibitem{Osborn1975}
J.~E.~Osborn,
\textit{Spectral approximation for
compact operators},
Math.\ Comp.\ \textbf{29} (1975), 712--725.

\end{thebibliography}

\end{document}
