\documentclass[12pt,a4paper]{amsart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{booktabs}

%%%─── Theorem environments
\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{conjecture}[theorem]{Conjecture}
\newtheorem{hypothesis}[theorem]{Hypothesis}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{problem}[theorem]{Open Problem}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}
\newtheorem*{remark*}{Remark}

%%%─── Macros
\newcommand{\norm}[1]{\|#1\|}
\newcommand{\ip}[2]{\langle #1,\, #2\rangle}
\newcommand{\Hlim}{\mathcal{H}_{\lim}}
\newcommand{\Lc}{\mathcal{L}_c}
\newcommand{\Pninf}{P_n^{(\infty)}}
\newcommand{\Pnck}{P_n^{(c_k)}}
\newcommand{\spc}[1]{\sigma(#1)}

\subjclass[2020]{47A10, 47B25, 42C05, 46B15, 34E20}
\keywords{Prolate spheroidal wave functions, spectral projection stability,
approximate eigenvector, strong operator convergence,
spectral gap, limiting operator, Riesz basis}

\begin{document}

\title[Spectral Projection Stability for the Limiting PSWF System]{%
  Spectral Projection Stability and the Bridge\\
  to Completeness for the Limiting PSWF Operator}

\author{[Author]}
\address{[Address]}
\email{[Email]}

\maketitle

%%══ 1. INTRODUCTION
\section{Introduction}
\label{sec:intro}

\subsection{Context and goal}

This paper occupies a specific position in the series:
it provides the bridge between
the strong operator convergence of Paper~X
and the completeness result of Paper~XIII.

Paper~X establishes that the PSWF concentration
operators $\mathcal{H}_{c_k}$ converge in the
strong operator topology (SOT) to a bounded
self-adjoint limit $\Hlim$.
Paper~XI constructs a diagonal subsequence
along which the eigenfunctions $\Phi_n^{(c_k)}$
converge weakly to limit vectors $\Phi_n^{(\infty)}$,
orthonormal and satisfying
$\Hlim\Phi_n^{(\infty)} = \lambda_n^{(\infty)}\Phi_n^{(\infty)}$.

\textbf{The gap that this paper fills.}
SOT convergence does \emph{not} imply norm
convergence of spectral projections
(Rem.~\ref{rem:strong-vs-norm}).
The difficulty is precise:
SOT convergence produces approximate eigenvectors
--- the vectors $\Phi_n^{(\infty)}$ satisfy
$\|(\mathcal{H}_{c_k}-\lambda_n^{(\infty)})
\Phi_n^{(\infty)}\|\to 0$ as $k\to\infty$ ---
but provides no control on the residual
\emph{relative to the spectral gap}.
Without such control, spectral isolation
cannot be invoked, and projection convergence
cannot be concluded.
This is exactly the gap this paper identifies
and bridges.

The central result (Thm.~\ref{thm:convergence})
establishes $\|\Pnck - \Pninf\|\to 0$
under two conditions:
\begin{itemize}
\item \hyperref[hyp:gap-structural]{Gap-S}:
  an effective asymptotic gap condition
  in the transition window $W_c$,
  $\lim_{c\to\infty} c^{-1/4}/g(c)=0$;
\item \hyperref[hyp:sot-faster]{SOT-Faster}:
  $\|(\mathcal{H}_{c_k}-\Hlim)\Phi_n^{(\infty)}\|
  =o(g(c_k))$ as $k\to\infty$,
  a local spectral stability condition
  (see Rem.~\ref{rem:sot-faster-resolvent}).
\end{itemize}
Neither condition requires any bound on
$\|\mathcal{H}_{c_k}-\Hlim\|$.

\subsection{Main results}

\begin{itemize}
\item \textbf{Theorem~\ref{thm:convergence}
  (Convergence):}
  Under Gap-S and SOT-Faster,
  $\|\Pnck - \Pninf\|\to 0$.

\item \textbf{Theorem~\ref{thm:rate}
  (Rate):}
  Under the additional
  \hyperref[hyp:sot-rate]{Hyp.~SOT-Rate},
  $\|\Pnck-\Pninf\|
  \le C C_\kappa c_k^{-1/4}/g(c_k)$,
  uniformly in $n\le\kappa c_k$.
\end{itemize}

\begin{remark}[Core contribution
  and abstract structure]
\label{rem:conditions}
Theorem~\ref{thm:convergence} is an
\emph{abstract operator-theoretic
implication}:
it holds for any sequence of bounded
self-adjoint operators satisfying the
stated hypotheses.
All PSWF-specific content of the paper
is encoded in Hypotheses~Gap-S and
SOT-Faster; the operator-theoretic
mechanism --- approximate eigenvector
$+$ spectral isolation $\Rightarrow$
projection convergence --- is
a rank-one specialisation of the
Davis--Kahan $\sin\Theta$ theorem
(Lem.~\ref{lem:approx-eig},
Rem.~\ref{rem:perturbation-mechanism}).
\end{remark}

\subsection{What this paper does \emph{not} claim}
\begin{itemize}
\item \textbf{Riesz-basis property}
  (Cor.~\ref{cor:riesz}): trivial from
  orthonormality in Paper~XI.
\item \textbf{Completeness}
  $\mathcal{K}=L^2(\mathbb{R})$: Paper~XIII.
\item \textbf{Proof of Gap-S}:
  Open Problem~\ref{prob:sharp-gc}
  (conjecture-level).
\item \textbf{Proof of SOT-Faster
  or SOT-Rate}:
  Open Problems~\ref{prob:sot-faster},
  \ref{prob:sot-rate}.
\end{itemize}

\subsection{Role in the series}
\begin{center}
\begin{tabular}{lll}
\toprule
Paper & Content & Status\\
\midrule
X   & SOT: $\mathcal{H}_c
  \xrightarrow{\mathrm{SOT}}\Hlim$ & Final\\
XI  & Diagonal system
  $\{\Phi_n^{(\infty)},\lambda_n^{(\infty)}\}$
  & Final\\
\textbf{XII} & \textbf{Projection stability;
  bridge to XIII} & \textbf{This paper}\\
XIII& Completeness $\mathcal{K}=L^2$
  & Planned\\
\bottomrule
\end{tabular}
\end{center}

\subsection*{Guide for the Referee}
\begin{itemize}
\item Central result: Thm.~\ref{thm:convergence};
  abstract structure in Rem.~\ref{rem:conditions};
  proof in Sec.~\ref{sec:proj}.
\item Key mechanism: Lem.~\ref{lem:approx-eig}
  (Davis--Kahan rank-one specialisation)
  and Rem.~\ref{rem:perturbation-mechanism}.
\item Eigenvalue matching:
  Lem.~\ref{lem:eigenvalue-matching}.
\item Hypotheses Gap-S
  (\ref{hyp:gap-structural}) and SOT-Faster
  (\ref{hyp:sot-faster}):
  Sec.~\ref{sec:gap};
  heuristic motivation: Sec.~\ref{sec:heuristic}.
\item Conditional rate Thm.~\ref{thm:rate}:
  SOT-Rate (\ref{hyp:sot-rate}),
  clearly flagged.
\item Riesz corollary explicitly trivial:
  Sec.~\ref{sec:riesz-trivial}.
\item All open problems tiered
  (conjecture vs.\ technical):
  Sec.~\ref{sec:open}.
\end{itemize}

\tableofcontents

%%══ 2. INPUT
\section{Input from Papers VIII, X, XI}
\label{sec:input}

\begin{lemma}[Inherited data]
\label{lem:inherited}
\begin{enumerate}[\rm(A)]
\item\label{A1}
  \emph{(VIII, Cor.~4.3)}
  $|\lambda_n^{(c)}-\lambda_n^{(\infty)}|
  \le C_\kappa c^{-1/4}$
  for $n\le\kappa c$, uniform in $n$.
\item\label{A2}
  \emph{(X, Thread~A)}
  $\mathcal{H}_{c_k}\xrightarrow{\mathrm{SOT}}\Hlim$,
  bounded self-adjoint:
  \begin{equation}\label{eq:sot}
    \|(\mathcal{H}_{c_k}-\Hlim)f\|\to 0
    \quad\forall\, f\in L^2.
  \end{equation}
  Paper~X provides no rate on \eqref{eq:sot}.
\item\label{A3}
  \emph{(XI, Lem.~3.1 + Thm.~5.1)}
  There exists $c_k\nearrow\infty$:
  $\ip{\Phi_m^{(\infty)}}{\Phi_n^{(\infty)}}=\delta_{mn}$,
  $\Hlim\Phi_n^{(\infty)}=\lambda_n^{(\infty)}\Phi_n^{(\infty)}$,
  $\lambda_n^{(c)}$ simple for all $n$, $c$.
\end{enumerate}
\end{lemma}

\begin{lemma}[Eigenvalue matching]
\label{lem:eigenvalue-matching}
Under \eqref{A1}, for each fixed $n$ and
all $k$ with $n\le\kappa c_k$:
\begin{equation}\label{eq:ev-matching}
  |\lambda_n^{(c_k)}-\lambda_n^{(\infty)}|
  \le C_\kappa\,c_k^{-1/4}.
\end{equation}
In particular, $\lambda_n^{(c_k)}\to
\lambda_n^{(\infty)}$ as $k\to\infty$,
and the spectral labeling along the
diagonal subsequence is consistent:
the $n$-th eigenvalue of $\mathcal{H}_{c_k}$
converges to the $n$-th eigenvalue of
$\Hlim$ in the ordering inherited from
Paper~XI.
\end{lemma}
\begin{proof}
Direct from \eqref{A1}.
\end{proof}

\begin{remark}[SOT does not imply
  projection norm convergence]
\label{rem:strong-vs-norm}
By~\cite[Ex.~VIII.7.14]{ReedSimon1975},
SOT convergence does not in general imply
norm convergence of spectral projections.
Gap-S and SOT-Faster supply precisely
the missing structure.
\end{remark}

%%══ 3. HYPOTHESES
\section{Hypotheses and Motivation}
\label{sec:gap}

\begin{hypothesis}[Effective asymptotic
  gap in transition window, Gap-S]
\label{hyp:gap-structural}
Let $W_c\subset\mathbb{N}$ denote the
\emph{transition window}: indices $n$
for which $\lambda_n^{(c)}\in(\delta,1-\delta)$
for some fixed $\delta>0$.
There exists $g\colon[1,\infty)\to(0,\infty)$
with
\begin{equation}\label{eq:gap-dominates}
  g(c)\to 0
  \quad\text{and}\quad
  \lim_{c\to\infty}\frac{c^{-1/4}}{g(c)} = 0,
\end{equation}
such that for all $c\ge 1$,
uniformly in $n\in W_c$:
\begin{equation}\label{eq:gap-s}
  |\lambda_n^{(c)}-\lambda_{n+1}^{(c)}|
  \ge g(c).
\end{equation}
\end{hypothesis}

\begin{remark}[Scope and effective
  interpretation of Gap-S]
\label{rem:gap-scope}
The restriction to $W_c$ is essential:
bulk eigenvalues (near $0$ or $1$)
have exponentially small gaps
not uniformly bounded below by $g(c)$,
but these indices do not appear in the
diagonal subsequence of Paper~XI.
Within the transition window,
Widom-type asymptotics~\cite{Widom1964}
predict average spacing $\sim(\log c)^{-1}$,
so Gap-S with $g(c)=(\log c)^{-1}$
is an \emph{effective asymptotic} condition
--- a lower bound on the typical gap
rather than a deterministic infimum ---
consistent with known PSWF spectral theory.
A rigorous deterministic lower bound
remains Open Problem~\ref{prob:sharp-gc}.
\end{remark}

\begin{remark}[Gap-S implies $c^{-1/4}=o(g(c))$]
\label{rem:gap-implies-o}
Condition \eqref{eq:gap-dominates} is
equivalent to $c^{-1/4}=o(g(c))$
as $c\to\infty$, cited explicitly in
the proof of Thm.~\ref{thm:convergence}.
\end{remark}

\begin{hypothesis}[Gap-dominated residual
  condition, SOT-Faster]
\label{hyp:sot-faster}
For each fixed $n$, along the diagonal
subsequence of Lem.~\ref{lem:inherited}:
\begin{equation}\label{eq:sot-faster}
  \varepsilon_n(k) :=
  \|(\mathcal{H}_{c_k}-\Hlim)
  \Phi_n^{(\infty)}\|
  = o(g(c_k))
  \quad\text{as }k\to\infty.
\end{equation}
\end{hypothesis}

\begin{remark}[SOT-Faster as local
  resolvent stability]
\label{rem:sot-faster-resolvent}
SOT (Paper~X) gives $\varepsilon_n(k)\to 0$,
but no rate.
SOT-Faster is quantitatively stronger:
it requires the operator residual on
the \emph{fixed limit eigenvector}
$\Phi_n^{(\infty)}$ to decay faster than
the spectral gap of the
\emph{approximating operator}
$\mathcal{H}_{c_k}$.
This is in spirit a
\emph{local resolvent stability condition}
at the eigenvalue $\lambda_n^{(c_k)}$:
it would follow, for example, from
uniform control of
$\|(\mathcal{H}_{c_k}-z)^{-1}
-(\Hlim-z)^{-1}\|$
on the $n$-th spectral scale,
which is norm-resolvent information
restricted to a single eigenvalue.
Proving this rigorously is
Open Problem~\ref{prob:sot-faster};
Sec.~\ref{sec:heuristic} presents
evidence consistent with it
in the PSWF setting.
\end{remark}

\begin{remark}[SOT-Faster is the
  core difficulty]
\label{rem:sot-faster-core}
SOT-Faster is not a technical side condition:
it encapsulates \emph{the entire difficulty}
of upgrading SOT convergence to
norm convergence of rank-one
spectral projections.
\end{remark}

\begin{hypothesis}[SOT rate on
  eigenvectors, SOT-Rate]
\label{hyp:sot-rate}
For each fixed $n$:
\begin{equation}\label{eq:sot-rate}
  \varepsilon_n(k) = O(c_k^{-1/4})
  \quad\text{as }k\to\infty.
\end{equation}
\end{hypothesis}

\begin{remark}[Logical hierarchy]
\label{rem:hyp-hierarchy}
\[
  \text{SOT-Rate}
  \underset{\eqref{eq:gap-dominates}}{\Longrightarrow}
  \text{SOT-Faster}
  \Longrightarrow
  \|P_n^{(c_k)}-P_n^{(\infty)}\|\to 0.
\]
SOT alone gives only $\varepsilon_n(k)\to 0$,
insufficient without comparison to $g(c_k)$.
\end{remark}

\begin{remark}[Plausibility of SOT-Rate]
\label{rem:sot-rate-plausible}
Expanding $\Phi_n^{(\infty)}$ in the
eigenbasis $\{\Phi_m^{(c_k)}\}$
of $\mathcal{H}_{c_k}$:
\begin{equation}\label{eq:formal-expansion}
  \varepsilon_n(k)^2
  = \sum_m
  |\lambda_m^{(c_k)}-\lambda_n^{(\infty)}|^2
  |\ip{\Phi_n^{(\infty)}}{\Phi_m^{(c_k)}}|^2.
\end{equation}
The diagonal term $m=n$ contributes
$\le C_\kappa^2 c_k^{-1/2}$ by \eqref{A1}.
If off-diagonal overlaps are small
for $m\ne n$ (consistent with Gap-S;
see Sec.~\ref{sec:heuristic}),
then the sum is dominated by $m=n$,
predicting $\varepsilon_n(k)=O(c_k^{-1/4})$.
This is formally consistent with SOT-Rate,
but does not constitute rigorous evidence;
it requires off-diagonal overlap estimates
(Open Problem~\ref{prob:off-diagonal}).
\end{remark}

\subsection{WKB layer}
\begin{theorem}[WKB insufficiency]
\label{thm:wkb-indeterminacy}
The WKB/Airy analysis of
$\Lc=-\tfrac{d}{dx}[(1-x^2)
\tfrac{d}{dx}]+c^2x^2$
does not determine a canonical scaling
for $|\lambda_{n+1}^{(c)}-\lambda_n^{(c)}|$
without global kernel information.
\end{theorem}
\begin{proof}
The transfer map $F_c$ has a sigmoid
profile near $\lambda=1/2$~\cite{Widom1964};
any algebraic rate $F_c'\asymp c^{-\beta}$
is a global claim not derivable from
$\Lc$ alone.
\end{proof}

%%══ 4. MAIN THEOREMS
\section{Main Theorems}
\label{sec:main}

\begin{theorem}[Spectral projection
  convergence]
\label{thm:convergence}
Assume Lem.~\ref{lem:inherited},
\hyperref[hyp:gap-structural]{Hyp.~Gap-S},
and \hyperref[hyp:sot-faster]{Hyp.~SOT-Faster}.
For each fixed $n$ with $n\in W_{c_k}$
for all large $k$:
\begin{equation}\label{eq:convergence}
  \|P_n^{(c_k)} - P_n^{(\infty)}\|\to 0
  \quad\text{as }k\to\infty.
\end{equation}
In particular:
\begin{enumerate}[\rm(i)]
\item $\Phi_n^{(c_k)}\to\Phi_n^{(\infty)}$
  strongly as $k\to\infty$, up to phase
  (Rem.~\ref{rem:phase}).
\item $\{\lambda_n^{(\infty)}\}$ has no
  accumulation point in $(0,1)$
  (Cor.~\ref{cor:nacc}).
\end{enumerate}
\end{theorem}

\begin{theorem}[Rate]
\label{thm:rate}
Assume additionally
\hyperref[hyp:sot-rate]{Hyp.~SOT-Rate}.
For all $n\le\kappa c_k$ with
$n\in W_{c_k}$, uniformly in $n$:
\begin{equation}\label{eq:rate-bound}
  \|P_n^{(c_k)} - P_n^{(\infty)}\|
  \le \frac{C\,C_\kappa\,c_k^{-1/4}}{g(c_k)},
\end{equation}
where $C>0$ is universal.
\end{theorem}

\begin{remark}[Phase convention]
\label{rem:phase}
For rank-one projections,
$\|P_n^{(c_k)}-P_n^{(\infty)}\|\to 0$
implies $\Phi_n^{(c_k)}
\to e^{i\theta_n^{(k)}}\Phi_n^{(\infty)}$
strongly.
Phase is fixed by
$\ip{\Phi_n^{(c_k)}}{\Phi_n^{(\infty)}}>0$,
valid for all large $k$.
\end{remark}

%%══ 5. PROOF
\section{Proof of the Main Theorems}
\label{sec:proj}

\begin{lemma}[Approximate eigenvector
  $\Rightarrow$ projection estimate;
  rank-one specialisation of
  Davis--Kahan $\sin\Theta$;
  cf.\ \cite{DavisKahan1970,Kato1966}]
\label{lem:approx-eig}
Let $A$ be a bounded self-adjoint operator
and $\lambda$ a simple eigenvalue of $A$
with
$\gamma:=\mathrm{dist}(\lambda,
\spc{A}\setminus\{\lambda\})>0$.
Let $\phi\in\mathcal{H}$, $\|\phi\|=1$,
with $\|(A-\lambda)\phi\|\le\varepsilon$.
Then:
\begin{equation}\label{eq:approx-eig-bound}
  \|(I-P_n^A)\phi\|
  \le\frac{\varepsilon}{\gamma},
\end{equation}
where $P_n^A$ is the rank-one spectral
projection of $A$ onto $\ker(A-\lambda)$.
\end{lemma}
\begin{proof}
Write $\phi = P_n^A\phi + (I-P_n^A)\phi$.
Since $(A-\lambda)P_n^A=0$:
$(A-\lambda)\phi = (A-\lambda)(I-P_n^A)\phi$.
On $\mathrm{ran}(I-P_n^A)$,
$\|(A-\lambda)^{-1}\|\le 1/\gamma$,
so
$\|(I-P_n^A)\phi\|
\le\gamma^{-1}\|(A-\lambda)\phi\|
\le\varepsilon/\gamma$.
\end{proof}

\begin{remark}[Perturbation mechanism]
\label{rem:perturbation-mechanism}
Lemma~\ref{lem:approx-eig} is a rank-one
specialisation of the Davis--Kahan
$\sin\Theta$ theorem~\cite{DavisKahan1970}:
\emph{an approximate eigenvector whose
residual is smaller than the spectral gap
must concentrate on the true eigenspace}.
The gap $\gamma$ in Lem.~\ref{lem:approx-eig}
is applied to $A=\mathcal{H}_{c_k}$,
not to $\Hlim$:
it is the gap of the \emph{approximating}
operator that provides spectral isolation,
and Gap-S supplies this via \eqref{eq:gap-s}.
SOT-Faster then ensures
$\varepsilon_n(k)/g(c_k)\to 0$
as $k\to\infty$, activating the mechanism.
\end{remark}

\begin{lemma}[SOT gives approximate
  eigenvectors, no rate]
\label{lem:sot-approx}
Under \eqref{A2}--\eqref{A3},
for each fixed $n$:
\begin{equation}\label{eq:approx-sot}
  \varepsilon_n(k)
  = \|(\mathcal{H}_{c_k}-\Hlim)
  \Phi_n^{(\infty)}\|\to 0
  \quad\text{as }k\to\infty.
\end{equation}
No rate on $\varepsilon_n(k)$ is claimed.
\end{lemma}
\begin{proof}
Since $\Hlim\Phi_n^{(\infty)}
=\lambda_n^{(\infty)}\Phi_n^{(\infty)}$,
we have $\varepsilon_n(k)
=\|(\mathcal{H}_{c_k}-\Hlim)
\Phi_n^{(\infty)}\|\to 0$
by SOT~\eqref{eq:sot} applied to the
fixed vector $f=\Phi_n^{(\infty)}$.
\end{proof}

\begin{proof}[Proof of
  Thm.~\ref{thm:convergence}]
Fix $n\in W_{c_k}$ for all large $k$.
By Lem.~\ref{lem:sot-approx} and the
triangle inequality, using the
explicit eigenvalue matching of
Lem.~\ref{lem:eigenvalue-matching}:
\begin{equation}\label{eq:eps-tilde}
  \tilde\varepsilon_n(k) :=
  \|(\mathcal{H}_{c_k}-\lambda_n^{(c_k)})
  \Phi_n^{(\infty)}\|
  \le \varepsilon_n(k)
  + |\lambda_n^{(\infty)}-\lambda_n^{(c_k)}|
  \le \varepsilon_n(k) + C_\kappa c_k^{-1/4}.
\end{equation}
Here $\tilde\varepsilon_n(k)$ is the
residual of $\Phi_n^{(\infty)}$ as
approximate eigenvector of
$\mathcal{H}_{c_k}$ at eigenvalue
$\lambda_n^{(c_k)}$;
the spectral parameter shift from
$\lambda_n^{(\infty)}$ to $\lambda_n^{(c_k)}$
is controlled by Lem.~\ref{lem:eigenvalue-matching}.
Dividing by $g(c_k)>0$ (Gap-S):
\begin{equation}\label{eq:ratio-to-zero}
  \frac{\tilde\varepsilon_n(k)}{g(c_k)}
  \le
  \underbrace{\frac{\varepsilon_n(k)}
    {g(c_k)}}_{\to\,0
    \;\text{(SOT-Faster)}}
  +
  \underbrace{\frac{C_\kappa c_k^{-1/4}}
    {g(c_k)}}_{\to\,0
    \;\text{(Rem.~\ref{rem:gap-implies-o})}}
  \to 0
  \quad\text{as }k\to\infty.
\end{equation}
Since $\lambda_n^{(c_k)}$ is a simple
eigenvalue of $\mathcal{H}_{c_k}$
with spectral gap $\ge g(c_k)$
(Gap-S for $n\in W_{c_k}$),
Lem.~\ref{lem:approx-eig} gives:
\begin{equation}\label{eq:proj-dev}
  \|(I-P_n^{(c_k)})\Phi_n^{(\infty)}\|
  \le\frac{\tilde\varepsilon_n(k)}{g(c_k)}
  \to 0
  \quad\text{as }k\to\infty.
\end{equation}
Write $P_n^{(c_k)}\Phi_n^{(\infty)}
=\alpha_k\Phi_n^{(c_k)}$ with
$\alpha_k
=\ip{\Phi_n^{(\infty)}}{\Phi_n^{(c_k)}}$;
then $|\alpha_k|^2
=1-\|(I-P_n^{(c_k)})\Phi_n^{(\infty)}\|^2
\to 1$.
By the rank-one projection identity
$\|P_u-P_v\|\le 2\|u-e^{i\theta}v\|$
(cf.~\cite[Ch.~1]{Bhatia1997}):
\[
  \|P_n^{(c_k)}-P_n^{(\infty)}\|
  \le 2\|\Phi_n^{(c_k)}
  -e^{i\arg\alpha_k}\Phi_n^{(\infty)}\|
  \to 0
  \quad\text{as }k\to\infty.
\]
\end{proof}

\begin{proof}[Proof of Thm.~\ref{thm:rate}]
Under SOT-Rate \eqref{eq:sot-rate}:
$\varepsilon_n(k)\le C' c_k^{-1/4}$,
so $\tilde\varepsilon_n(k)
\le(C'+C_\kappa)c_k^{-1/4}$.
With $\gamma=g(c_k)$,
Lem.~\ref{lem:approx-eig} and the
rank-one identity yield \eqref{eq:rate-bound}
uniformly for $n\le\kappa c_k$,
$n\in W_{c_k}$.
\end{proof}

\begin{corollary}[No accumulation
  of $\lambda_n^{(\infty)}$]
\label{cor:nacc}
Under Gap-S and \eqref{A1},
$\{\lambda_n^{(\infty)}\}$ has no
accumulation point in $(0,1)$.
\end{corollary}
\begin{proof}
Eigenvalues $\lambda_n^{(c)}$ are strictly
ordered in $n$~\cite{Slepian1961}.
Fix $\mu\in(0,1)$, $r>0$;
choose $k_0$ with
$C_\kappa c_{k_0}^{-1/4}<r/2$.
Then $\lambda_n^{(\infty)}\in(\mu-r,\mu+r)$
implies $\lambda_n^{(c_{k_0})}
\in(\mu-3r/2,\mu+3r/2)$.
By Gap-S on $W_{c_{k_0}}$,
at most $\lceil 3r/g(c_{k_0})\rceil<\infty$
eigenvalues lie in this interval.
\end{proof}

%%══ 6. RIESZ COROLLARY
\section{The Riesz-Basis Corollary
  (Explicit Trivialization)}
\label{sec:riesz-trivial}

\begin{remark}
The property below follows from
orthonormality (\eqref{A3}) alone
and is not the contribution of this paper.
\end{remark}

\begin{corollary}[Riesz basis --- trivial]
\label{cor:riesz}
$\{\Phi_n^{(\infty)}\}$ is a Riesz basis
of $\mathcal{K}:=\overline{\mathrm{span}}
\{\Phi_n^{(\infty)}\}$ with bounds $A=B=1$.
\end{corollary}
\begin{proof}
Orthonormality from \eqref{A3}.
\end{proof}

%%══ 7. CANONICALIZATION
\section{Canonicalization (Conditional)}
\label{sec:canonical}

\begin{lemma}[Same $\mathcal{K}$,
  conditional]
\label{lem:same-K}
Assume additionally:
\begin{hypothesis}[Full family convergence]
\label{hyp:full-family}
The spectral measures of $\mathcal{H}_{c_k}$
converge weak-$*$ to those of $\Hlim$.
\end{hypothesis}
Then any two diagonal subsequences
satisfying Gap-S yield the same
closed span $\mathcal{K}$.
\end{lemma}
\begin{proof}
Both subspaces are closed
$\Hlim$-invariant;
under Hyp.~\ref{hyp:full-family}
they generate the same spectral measure.
\end{proof}

\begin{theorem}[Canonicalization,
  conditional]
\label{thm:canonical}
Under Gap-S and Hyp.~\ref{hyp:full-family},
$\Hlim$ is canonical on $\mathcal{K}$
up to unitary equivalence.
\end{theorem}

%%══ 8. HEURISTIC
\section{Heuristic Motivation of Gap-S
  and SOT-Faster}
\label{sec:heuristic}

\textbf{Non-rigorous throughout.}

\subsection{Sigmoid transition (Widom)}

Widom-type asymptotics~\cite{Widom1964}
predict a sigmoidal eigenvalue profile
in the transition window:
\[
  \lambda_n^{(c)}\approx
  \tfrac{1}{1+e^{\pi(n-2c/\pi)/\sigma(c)}},
  \quad\sigma(c)\asymp\log c.
\]
This predicts the correct qualitative
behavior and is consistent with Gap-S
at scale $g(c)\sim(\log c)^{-1}$.

\subsection{Gap estimate}

If $\rho(c)\asymp\log c$ eigenvalues
occupy the transition window $W_c$,
the average spacing is
$\sim(\log c)^{-1}\gg c^{-1/4}$,
strongly suggesting Gap-S.
A rigorous lower bound requires
Plancherel--Rotach or determinantal
kernel methods~\cite{Widom1964,Osipov2013}.

\subsection{SOT-Faster plausibility}

From \eqref{eq:formal-expansion},
if off-diagonal overlaps satisfy
$|\ip{\Phi_n^{(\infty)}}{\Phi_m^{(c_k)}}|
\to 0$ for $m\ne n$
(consistent with spectral isolation
under Gap-S),
then the sum is dominated by $m=n$,
predicting $\varepsilon_n(k)=O(c_k^{-1/4})$.
This is formally consistent with
SOT-Faster (and the stronger SOT-Rate),
but does not constitute rigorous evidence;
making it precise requires control of
off-diagonal overlaps
(Open Problem~\ref{prob:off-diagonal}).

\begin{remark}[Heuristic status]
Section~\ref{sec:heuristic} predicts the
correct qualitative scaling and provides
evidence \emph{consistent with}
Gap-S and SOT-Faster;
it does not constitute a rigorous proof
of either.
\end{remark}

%%══ 9. OPEN PROBLEMS
\section{Open Problems}
\label{sec:open}

\noindent The open problems below are
tiered by perceived depth.

\medskip
\noindent\textbf{Conjecture-level
(require new asymptotic/spectral methods):}

\begin{problem}[Sharp gap scale]
\label{prob:sharp-gc}
Prove $|\lambda_n^{(c)}-\lambda_{n+1}^{(c)}|
\ge g(c)$ for $n\in W_c$
with $g(c)\sim(\log c)^{-1}$ rigorously;
would establish Gap-S as a theorem.
\end{problem}

\begin{problem}[SOT-Faster]
\label{prob:sot-faster}
Prove $\varepsilon_n(k)=o(g(c_k))$
as $k\to\infty$;
would make Thm.~\ref{thm:convergence}
fully unconditional.
Equivalently (by Rem.~\ref{rem:sot-faster-resolvent}):
prove local resolvent stability of
$\mathcal{H}_{c_k}$ at the $n$-th
eigenvalue scale.
\end{problem}

\medskip
\noindent\textbf{Technical-level
(more tractable, conditional on above):}

\begin{problem}[SOT-Rate]
\label{prob:sot-rate}
Prove $\varepsilon_n(k)=O(c_k^{-1/4})$
as $k\to\infty$;
stronger than SOT-Faster
(implies it via Rem.~\ref{rem:hyp-hierarchy})
and would justify the rate in
Thm.~\ref{thm:rate}.
\end{problem}

\begin{problem}[Off-diagonal overlaps]
\label{prob:off-diagonal}
Estimate
$|\ip{\Phi_n^{(\infty)}}{\Phi_m^{(c_k)}}|$
for $m\ne n$ rigorously;
would supply a route to
Open Problem~\ref{prob:sot-rate}.
\end{problem}

\begin{problem}[Full family convergence]
\label{prob:full-family}
Establish Hyp.~\ref{hyp:full-family};
would make Lem.~\ref{lem:same-K}
unconditional.
\end{problem}

\begin{problem}[Completeness]
\label{prob:completeness}
$\mathcal{K}=L^2(\mathbb{R})$?
Paper~XIII.
\end{problem}

%%%─── BIBLIOGRAPHY
\begin{thebibliography}{99}

\bibitem{PaperXI}
[Author], \textit{Spectral structure
for the limiting PSWF operator},
Paper~XI, 2026.

\bibitem{PaperX}
[Author], \textit{Mosco form convergence
and the bridge theorem for PSWF
concentration operators},
Paper~X, 2026.

\bibitem{PaperVIII}
[Author], \textit{Non-cancellation control
and the sharp $c^{-1/2}$ lower bound},
Paper~VIII, 2026.

\bibitem{Slepian1961}
D.~Slepian and H.~O.~Pollak,
\textit{Prolate spheroidal wave functions,
Fourier analysis and uncertainty~I},
Bell Syst.\ Tech.\ J.\ \textbf{40}
(1961), 43--63.

\bibitem{Osipov2013}
A.~Osipov, V.~Rokhlin, and H.~Xiao,
\textit{Prolate Spheroidal Wave Functions
of Order Zero}, Springer, 2013.

\bibitem{Widom1964}
H.~Widom,
\textit{Asymptotic behavior of the
eigenvalues of certain integral
equations~II},
Arch.\ Ration.\ Mech.\ Anal.\ \textbf{17}
(1964), 215--229.

\bibitem{DavisKahan1970}
C.~Davis and W.~M.~Kahan,
\textit{The rotation of eigenvectors
by a perturbation~III},
SIAM J.\ Numer.\ Anal.\ \textbf{7}
(1970), 1--46.

\bibitem{Bhatia1997}
R.~Bhatia,
\textit{Matrix Analysis},
Springer, 1997.

\bibitem{Kato1966}
T.~Kato,
\textit{Perturbation Theory for
Linear Operators}, Springer, 1966.

\bibitem{ReedSimon1975}
M.~Reed and B.~Simon,
\textit{Methods of Modern Mathematical
Physics, Vols.~I--II},
Academic Press, 1972--1975.

\bibitem{GohbergKrein1969}
I.~Gohberg and M.~Krein,
\textit{Introduction to the Theory of
Linear Nonselfadjoint Operators},
AMS, 1969.

\bibitem{DLMF2023}
F.~W.~J.~Olver et al.\ (eds.),
\textit{NIST Digital Library of
Mathematical Functions},
Release 1.1.10, 2023.

\end{thebibliography}

\end{document}
