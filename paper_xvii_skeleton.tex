\documentclass[12pt,a4paper]{amsart}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{enumitem}
\usepackage{hyperref}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{assumption}[theorem]{Assumption}
\newtheorem{problem}[theorem]{Open Problem}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\Rig}{\textup{[Rig]}}
\newcommand{\Cond}{\textup{[Cond]}}
\newcommand{\Open}{\textup{[Open]}}
\newcommand{\Skel}{\textup{[Skeleton]}}

\begin{document}

\title[A$_2$-Stability for PSWF Amplitudes]{%
  A$_2$-Stability for Prolate Spheroidal Wave Function Amplitudes\\
  in the Semiclassical Limit:\\
  Proof Architecture Skeleton}

\author{[Author]}

\begin{abstract}
This is the \emph{Level~0--1 proof architecture skeleton} for Paper~XVII.
It fixes the logical structure and identifies the critical bottlenecks
before full LaTeX elaboration.

\medskip
\noindent\textbf{Objective.}
Verify Hypothesis~\textup{hyp:rigidity} from Paper~XVI
for PSWF amplitudes:
the PSWF amplitude satisfies Assumption~A2 of Paper~XVI
(conditions (A1)--(A3)) uniformly as $h := c^{-1} \to 0$.

\medskip
\noindent\textbf{Central new objects.}
The \emph{Olver--FIO Lift} is split into two logically independent lemmas:
\begin{itemize}
\item Lemma~\ref{lem:olver-fio-lift-micro}
  (\emph{Microlocal Structure}):
  Lagrangian structure and A$_2$-fold from Olver $+$ Sturm--Liouville.
  Status: \Cond.
\item Lemma~\ref{lem:uniform-projection}
  (\emph{Uniform Projection}):
  the projection $\pi_h^+\colon\Lambda_h^+\to T^*(-1,1)$
  on the positive-momentum branch is uniformly transversal.
  Status: \Cond\ (follows from Lemma~\ref{lem:diff-olver}).
\end{itemize}
The \emph{Differentiated Olver Estimate}
(Lemma~\ref{lem:diff-olver}) is the main technical contribution.
Its proof proceeds via the \emph{integral representation}
\[
  \phi(x;h)
  = \mathrm{sgn}(x-x_0(h))\cdot
    \Bigl(\tfrac{3}{2}\int_{x_0(h)}^x p(s;h)\,ds\Bigr)^{2/3},
  \quad
  p(x;h) := \sqrt{E_n(h)-V(x)},
\]
reducing all $C^1$-control of $\phi$ to
uniform control of the momentum function $p(x;h)$ and its integral.
No ODE, no Gr\"onwall inequality, and no Wasow-type expansion is needed.

\medskip
\noindent\textbf{Strategy.}
Primary: Olver turning-point analysis (Ch.~10--11 of \cite{Olver1974}).
Secondary: FIO reinterpretation of Olver expansions.
Auxiliary: Sturm--Liouville spectral theory; boundary-away cutoff.
\end{abstract}

\maketitle

\tableofcontents

%% ============================================================
\section{Input from Paper~XVI}
\label{sec:input}
%% ============================================================

The following are used as \emph{black boxes} (not reproved here):

\begin{itemize}
\item \textbf{thm:CFU-geom} (XVI):
  A$_2$-fold $\Rightarrow$ Airy normal form via $\kappa$.
\item \textbf{thm:CFU-symbol} (XVI):
  $I^{-1/4,0}(\Lambda_0,\Lambda_1) \to S^{-1/2}_{1,0}(h)$
  under $\kappa$, uniform in $h$.
\item \textbf{Assumption~A2} (XVI):
  \begin{enumerate}[{\rm(A1)}]
  \item Single nondegenerate fold:
    $c_3(h) \ge \delta_0 > 0$ uniformly.
  \item CFU admissibility:
    $|\det(dt/d\beta)| \ge \delta_1 > 0$ uniformly in $h$.
  \item Unique fold on $\mathrm{supp}\,\chi$.
  \end{enumerate}
\item \textbf{hyp:rigidity} (XVI):
  Airy class $\Rightarrow$ A$_2$-fold uniform in $h$;
  verified here for PSWF.
\end{itemize}

\begin{remark}[What Airy structure does not imply]
\label{rem:airy-not-cfu}
Airy normal form is local and pointwise.
CFU admissibility is global and uniform in $h$.
The implication Airy $\Rightarrow$ CFU admissibility
requires the branch selection of
Proposition~\ref{prop:branch-selection},
Assumption~\ref{ass:boundary-away},
and the integral representation of
Lemma~\ref{lem:diff-olver}.
\end{remark}

%% ============================================================
\section{PSWF Setup and Standing Assumptions}
\label{sec:pswf}
%% ============================================================

\begin{definition}[PSWF as semiclassical eigenfamily]\label{def:pswf}
$\psi_n(x;c)$ are eigenfunctions of
$\mathcal{L}_c = -\tfrac{d}{dx}[(1-x^2)\tfrac{d}{dx}] + c^2x^2$.
With $h = c^{-1}$:
\[
  h^2\mathcal{L}_{h^{-1}}
  = -h^2\tfrac{d}{dx}[(1-x^2)\tfrac{d}{dx}] + x^2,
\]
a semiclassical Schr\"odinger operator with effective potential
$V(x) = x^2/(1-x^2)$.
The turning points $x_\pm(h)$ solve $V(x_\pm) = E_n(h)$,
where $E_n(h) := \chi_n(h^{-1})\cdot h^2$.
\end{definition}

\begin{remark}[Eigenvalue branch and momentum limit]
\label{rem:eigenbranch}
$E_n(h) \to \lambda_n \in (0,1)$ smoothly and monotonically as $h\to 0$.
Define the limiting momentum:
$p_0(x) := \sqrt{\lambda_n - V(x)}$.
On $\mathrm{supp}\,\chi$ (away from turning points;
Assumption~\ref{ass:boundary-away}),
the positive infimum
\[
  \inf_{x\in K_\varepsilon} p_0(x) \;\ge\; c_\varepsilon > 0
\]
holds for every $\varepsilon > 0$,
since $\lambda_n > V(x_\pm^{(0)})$ at the limiting turning points
and $K_\varepsilon$ excludes an $\varepsilon$-neighborhood of $x_0^{(0)}$.
\end{remark}

\begin{assumption}[Boundary-away cutoff]
\label{ass:boundary-away}
Fix $\delta_{\rm bdy}>0$.
All constructions are localized to
$\mathrm{supp}\,\chi \subset [-1+\delta_{\rm bdy},\,1-\delta_{\rm bdy}]$,
where $\chi\in C_c^\infty(-1,1)$ is fixed independently of $h$.
On this set: $V, V', V'' \in L^\infty$ uniformly,
ruling out boundary-layer effects and $V \to \infty$.
All statements below are relative to this assumption.
\end{assumption}

%% ============================================================
\section{Olver's Uniform Asymptotic Scheme}
\label{sec:olver}
%% ============================================================

\subsection{What Olver provides}

Olver \cite{Olver1974} (Ch.~10--11) provides uniform
asymptotics matching WKB and Airy regimes:
\[
  \psi_n(x;h)
  \sim h^{-1/4}\,a(x;h)\,
  \mathrm{Ai}(h^{-2/3}\phi(x;h))
  + \mathcal{O}(h^\infty),
\]
with uniform error bounds on $\mathrm{supp}\,\chi$
(guaranteed by Assumption~\ref{ass:boundary-away}).

\subsection{What Olver does not provide}

\begin{remark}[Olver--CFU gap]
\label{rem:olver-gap}
Olver does not provide:
a Lagrangian distribution framework;
FIO-class membership;
global geometry of $\Lambda_h$;
uniform transversality of $\pi_h$;
or uniform $C^1$-control of $\phi(x;h)$.
Classical warning:
$f_h(x) = x + h\sin(x/h)$ converges uniformly
but $f_h'(x) = 1 + \cos(x/h)$ does not ---
pointwise convergence of $\phi$ does not imply
uniform convergence of $\phi'$.
\end{remark}

%% ============================================================
\section{Branch Selection}
\label{sec:branch}
%% ============================================================

\begin{remark}[WKB two-branch structure]
\label{rem:two-branch}
In the classically allowed region $E_n(h) > V(x)$:
\[
  \Lambda_h = \Lambda_h^+ \cup \Lambda_h^-,
  \quad
  \Lambda_h^\pm = \{\xi = \pm p(x;h)\}.
\]
Both branches project to the same base interval;
$\pi_h$ is not injective on $\Lambda_h$.
Condition (P2) is false for $\Lambda_h$ as a whole
and must be achieved by explicit construction.
\end{remark}

\begin{proposition}[Branch selection, \Cond]
\label{prop:branch-selection}
There exists $\delta_\xi > 0$ and a
semiclassical pseudodifferential operator
$\Pi^+ = \mathrm{Op}_h(\sigma^+)$
with $\sigma^+\in C_c^\infty(T^*(-1,1))$
supported on $\{\xi\ge \delta_\xi/2\}$,
such that:
\begin{enumerate}[{\rm(i)}]
\item $\chi\,\psi_n(\cdot;h) = \Pi^+(\chi\psi_n) + \mathcal{O}(h^\infty)$
  in $L^2$, uniformly in $h$.
\item $\Pi^+(\chi\psi_n)$ is a Lagrangian distribution
  on $\Lambda_h^+$ alone.
\end{enumerate}
Here $\delta_\xi := \tfrac{1}{2}\inf_{h\in(0,h_0],\,x\in\mathrm{supp}\,\chi} p(x;h) > 0$,
where positivity follows from Assumption~\ref{ass:boundary-away}
and the fact that $E_n(h) > V(x)$ on $\mathrm{supp}\,\chi$
uniformly for small $h$.
\end{proposition}

\begin{remark}[Proof]
$\delta_\xi > 0$ because
$p(x;h)^2 = E_n(h) - V(x) \ge \lambda_n - \|V\|_{L^\infty} - o(1) > 0$
on $\mathrm{supp}\,\chi$ (strict inequality preserved for $h\le h_0$).
The $\mathcal{O}(h^\infty)$ error is standard
$h$-pseudodifferential calculus \cite{Zworski2012}.
Status: \Cond.
\end{remark}

%% ============================================================
\section{Global Geometry: Conditions (P1) and (P3)}
\label{sec:phi-conditions}
%% ============================================================

\begin{remark}[(P1) and (P3) after branch selection]
\label{rem:p-status}
\begin{enumerate}[{\rm(P1)}]
\item \textit{(Global generating function.)}\;
  On $\Lambda_h^+$, $\xi = p(x;h) > 0$ is single-valued.
  Hence $\phi(x;h)$ (via the integral representation below)
  is a global generating function.
  Status: \Cond.
\item[{\rm(P3)}]
  \textit{(No secondary caustics.)}\;
  On $\mathrm{supp}\,\chi \subset
  [-1+\delta_{\rm bdy}, 1-\delta_{\rm bdy}]$,
  the only zero of $p(x;h)$ is the turning point $x_0(h)$.
  Boundary singularities of $V$ are excluded.
  Mode interactions excluded by the SL spectral gap
  (eigenvalue separation estimate required).
  Status: \Cond.
\end{enumerate}
\end{remark}

%% ============================================================
\section{The Olver--FIO Lift}
\label{sec:lift}
%% ============================================================

\subsection{Lemma 4.1a: Microlocal Structure}

\begin{lemma}[Microlocal Structure, \Cond]
\label{lem:olver-fio-lift-micro}
Under Assumption~\ref{ass:boundary-away}
and the smooth-eigenbranch hypothesis:
\begin{enumerate}[{\rm(i)}]
\item $\Pi^+(\chi\psi_n(\cdot;h))\in I^{-1/4,0}(\Lambda_h^+)$
  with phase $\phi(x;h)$.
\item $\Lambda_h^+$ carries a single A$_2$-fold at $(x_0,0)$
  with $c_3(h)\ge\delta_0>0$ uniformly.
\end{enumerate}
\end{lemma}

\subsection{Lemma 4.1b: Uniform Projection}

\begin{lemma}[Uniform Projection, \Cond]
\label{lem:uniform-projection}
Assume Lemma~\ref{lem:olver-fio-lift-micro},
Proposition~\ref{prop:branch-selection},
(P1), (P3), and Lemma~\ref{lem:diff-olver}.
Then, on $K_\varepsilon = \mathrm{supp}\,\chi\setminus U_\varepsilon$:
\[
  \inf_{h\in(0,h_0],\,x\in K_\varepsilon}|\phi'(x;h)| \ge \delta > 0,
\]
hence $|\det\,d\pi_h^+|\ge\delta_2>0$ uniformly on $\Lambda_h^+\setminus U_\varepsilon$.
\end{lemma}

\begin{remark}[Reduction to Lemma~\ref{lem:diff-olver}]
Away from the turning point:
$\phi'(x;h) = p(x;h)/\phi(x;h)$.
Numerator: $p(x;h) > 0$ uniformly
(Proposition~\ref{prop:branch-selection}).
Denominator: $|\phi(x;h)| \ge c_\varepsilon' > 0$
(Lemma~\ref{lem:diff-olver}(C)).
Uniform convergence $\phi'\to\phi_0'$ from Lemma~\ref{lem:diff-olver}(D).
Status: \Cond.
\end{remark}

%% ============================================================
\section{The Differentiated Olver Estimate}
\label{sec:diff-olver}
%% ============================================================

\begin{lemma}[Differentiated Olver Estimate, \Cond]
\label{lem:diff-olver}
\emph{(Main technical lemma of Paper~XVII.)}
Assume Assumption~\ref{ass:boundary-away}
and the smooth-eigenbranch hypothesis.
Fix $\varepsilon > 0$ and let
$K_\varepsilon := \mathrm{supp}\,\chi\setminus U_\varepsilon$,
where $U_\varepsilon$ is an open $\varepsilon$-neighborhood of $x_0(h)$.
Define:
\[
  p(x;h) := \sqrt{E_n(h)-V(x)},
  \qquad
  I(x;h) := \int_{x_0(h)}^x p(s;h)\,ds,
\]
\[
  \phi(x;h) := \mathrm{sgn}(x-x_0(h))\cdot
  \Bigl(\tfrac{3}{2}\,I(x;h)\Bigr)^{2/3},
\]
where the positive branch of $\sqrt{\,\cdot\,}$ is chosen,
consistent with the branch selection of
Proposition~\ref{prop:branch-selection}.
Then the following hold:

\begin{enumerate}[{\rm(A)}]
\item \textit{(Uniform convergence of $p$, with Lipschitz bound.)}
  On $K_\varepsilon$:
  \[
    \inf_{h\in(0,h_0],\,x\in K_\varepsilon} p_0(x) \;\ge\; c_\varepsilon > 0,
  \]
  since $K_\varepsilon$ is bounded away from both the turning point
  (where $p_0 = 0$) and the endpoints $\pm 1$
  (Assumption~\ref{ass:boundary-away}).
  Since $t\mapsto\sqrt{t}$ is Lipschitz on $[c_\varepsilon^2,\infty)$
  with constant $L_\varepsilon = (2c_\varepsilon)^{-1}$:
  \[
    \|p(\cdot;h) - p_0\|_{C^0(K_\varepsilon)}
    \;\le\; L_\varepsilon \cdot |E_n(h) - \lambda_n|
    \;=\; \mathcal{O}(h).
  \]

\item \textit{(Uniform convergence of $I$.)}
  \[
    \|I(\cdot;h) - I_0\|_{C^0(K_\varepsilon)} \to 0
    \quad\text{as } h\to 0,
  \]
  where $I_0(x) = \int_{x_0^{(0)}}^x p_0(s)\,ds$.
  Proof: $\|I(\cdot;h)-I_0\|\le\|p(\cdot;h)-p_0\|_{C^0}\cdot|K_\varepsilon|
  + p_0^{\max}\cdot|x_0(h)-x_0^{(0)}|$,
  both $\mathcal{O}(h)$ by (A) and the smooth eigenbranch.

\item \textit{(Denominator bounded away from zero.)}
  On $K_\varepsilon$, since $p > 0$ and
  $|x - x_0(h)| \ge \varepsilon/2$ for $h$ small:
  \[
    I(x;h) = \int_{x_0(h)}^x p(s;h)\,ds
    \;\ge\; \tfrac{\varepsilon}{2}\cdot
    \inf_{s\in K_\varepsilon} p(s;h)
    \;\ge\; \tfrac{\varepsilon}{2}\cdot c_\varepsilon' > 0
  \]
  uniformly in $h$ (where $c_\varepsilon' > 0$ follows from (A)
  for $h$ small).
  Hence
  $|\phi(x;h)| = (\tfrac{3}{2}|I(x;h)|)^{2/3}
  \ge (\tfrac{3}{4}\varepsilon c_\varepsilon')^{2/3} =: c_\varepsilon'' > 0$
  uniformly.

\item \textit{(Uniform $C^1$-control via quotient estimate.)}
  On $K_\varepsilon$:
  \[
    \phi'(x;h) = \frac{p(x;h)}{\phi(x;h)},
    \qquad
    \phi_0'(x) = \frac{p_0(x)}{\phi_0(x)}.
  \]
  Therefore:
  \begin{align*}
    |\phi'(x;h) - \phi_0'(x)|
    &= \Bigl|\frac{p(x;h)}{\phi(x;h)}-\frac{p_0(x)}{\phi_0(x)}\Bigr| \\
    &\le
    \frac{|p(x;h)-p_0(x)|}{|\phi(x;h)|}
    +
    p_0(x)\cdot
    \frac{|\phi(x;h)-\phi_0(x)|}{|\phi(x;h)|\cdot|\phi_0(x)|}.
  \end{align*}
  First term: $\le L_\varepsilon|E_n(h)-\lambda_n|/c_\varepsilon'' \to 0$ by (A),(C).
  Second term: $\to 0$ uniformly by (B),(C).
  Consequently:
  \[
    \sup_{x\in K_\varepsilon}|\phi'(x;h)-\phi_0'(x)| \to 0
    \quad\text{as } h\to 0.
  \]
  Since $\phi_0'(x) = p_0(x)/\phi_0(x) \ge c_\varepsilon/c_\varepsilon'' > 0$
  on $K_\varepsilon$:
  \[
    \inf_{h\in(0,h_0],\,x\in K_\varepsilon}|\phi'(x;h)|
    \;\ge\; \tfrac{1}{2}\inf_{K_\varepsilon}|\phi_0'| > 0.
  \]
\end{enumerate}
\end{lemma}

\begin{remark}[Why this proof is robust]
\label{rem:no-gronwall}
The argument uses only:
\begin{enumerate}[(i)]
\item Smooth convergence $E_n(h)\to\lambda_n$ (eigenbranch).
\item $V\in L^\infty$ on $\mathrm{supp}\,\chi$ (Assumption~\ref{ass:boundary-away}).
\item Lipschitz continuity of $t\mapsto\sqrt{t}$ on $[c_\varepsilon^2,\infty)$.
\item Monotonicity of $I(x;h)$ in $x$ (since $p > 0$)
  giving the lower bound on $|I|$ from the distance to the turning point.
\item Elementary quotient estimates.
\end{enumerate}
\emph{No} differentiated ODE, \emph{no} Gr\"onwall inequality,
\emph{no} Wasow-type expansion is required.
The key structural point:
$\phi$ has a zero of order $3/2$ at $x_0$, but
$\phi' = p/\phi$ remains controlled \emph{outside}
$U_\varepsilon$ because both $p \ge c_\varepsilon' > 0$
and $|\phi| \ge c_\varepsilon'' > 0$ hold there uniformly.
\end{remark}

%% ============================================================
\section{Derivation of Assumption~A2}
\label{sec:a2-derivation}
%% ============================================================

\begin{proposition}[A2 from all inputs, \Cond]
\label{prop:a2-from-lemmas}
Under Lemma~\ref{lem:olver-fio-lift-micro},
Proposition~\ref{prop:branch-selection},
(P1), (P3), and Lemma~\ref{lem:diff-olver}:
\[
  |\det(dt/d\beta)| \ge \delta_1 > 0
  \quad\text{uniformly in } h\in(0,h_0].
\]
\end{proposition}

\begin{proof}[Proof sketch]
Restrict to $\Lambda_h^+$ (Proposition~\ref{prop:branch-selection}).
By (P1), $\phi$ is a global generating function;
by (P3), no secondary caustics on $\mathrm{supp}\,\chi$.
The CFU map $\beta\mapsto t(\beta,u;h)$ is locally
equivalent to $\pi_h^+|_{\Lambda_h^+}$,
so $|\det(dt/d\beta)|$ is controlled by $|\phi'(x;h)|$.
Lemma~\ref{lem:diff-olver}(D) gives
$|\phi'(x;h)|\ge\delta>0$ uniformly.
\end{proof}

%% ============================================================
\section{Status Table}
\label{sec:status}
%% ============================================================

\begin{center}
\begin{tabular}{lll}
\hline
Statement & Status & Source \\
\hline
(A3) unique fold & \Rig & Sturm--Liouville \\
Airy scaling locally & \Rig & Olver Ch.~11 \\
Boundary-away hypothesis & \Rig\ (by assumption) & Ass.~\ref{ass:boundary-away} \\
$E_n(h)\to\lambda_n$ smoothly & \Cond & SL analyticity \\
$\inf_{K_\varepsilon} p_0 \ge c_\varepsilon>0$ & \Cond & Rem.~\ref{rem:eigenbranch}, $K_\varepsilon$ def. \\
(A1) $c_3(h)\ge\delta_0$ & \Cond & Olver $+$ eigenbranch \\
FIO-class membership & \Cond & Lem.~\ref{lem:olver-fio-lift-micro} \\
$\delta_\xi>0$ (branch gap) & \Cond & Ass.~\ref{ass:boundary-away} \\
(P2) branch selection & \Cond & Prop.~\ref{prop:branch-selection} \\
(P1) global gen.\ function & \Cond & single branch $+$ $p>0$ \\
(P3) no secondary caustics & \Cond & Ass.~\ref{ass:boundary-away} $+$ SL gap \\
(A) Unif.\ conv.\ of $p$, Lipschitz bound & \Cond & Lem.~\ref{lem:diff-olver}(A) \\
(B) Unif.\ conv.\ of $I$ & \Cond & Lem.~\ref{lem:diff-olver}(B) \\
(C) $|\phi|\ge c_\varepsilon''>0$ on $K_\varepsilon$ & \Cond & Lem.~\ref{lem:diff-olver}(C) \\
(D) Unif.\ $\phi'\to\phi_0'$;\ $\inf|\phi'|\ge\delta$ & \Cond & Lem.~\ref{lem:diff-olver}(D) \\
(A2) Jacobian bound & \Cond & Prop.~\ref{prop:a2-from-lemmas} \\
\textup{hyp:rigidity} for PSWF & \Cond & (A1)--(A3) $+$ chain above \\
\hline
\end{tabular}
\end{center}

\begin{remark}[Logical closure]
All items are \Cond\ or \Rig.
The proof architecture is logically closed.
Paper~XVII supplies the full proofs of
Lemma~\ref{lem:diff-olver}
and Proposition~\ref{prop:branch-selection}.
\end{remark}

%% ============================================================
\section{Target Theorem}
\label{sec:target}
%% ============================================================

\begin{theorem}[PSWF A$_2$-stability, \Cond]
\label{thm:xvii-main}
Under Assumption~\ref{ass:boundary-away}
and the smooth-eigenbranch hypothesis,
the PSWF amplitude satisfies Assumption~A2 of Paper~XVI
uniformly as $h\to 0$.
Consequently, the CFU reduction of Paper~XVI
applies to the PSWF family and
\textup{hyp:rigidity} (XVI) holds.
\end{theorem}

\begin{remark}[Logical dependency chain]
\label{rem:chain}
\[
  \underbrace{E_n(h)\to\lambda_n,\;
  V\in L^\infty\ (\text{Ass.}~\ref{ass:boundary-away})}_{{\rm [Rig/Cond]}}
  \;\Rightarrow\;
  \underbrace{\text{Lem.}~\ref{lem:diff-olver}\,(A){-}(D)}_{{\rm [Cond]}}
  \;+\;
  \underbrace{\text{Prop.}~\ref{prop:branch-selection}}_{{\rm [Cond]}}
  \;\Rightarrow\;
  \underbrace{\text{Lem.}~\ref{lem:uniform-projection}}_{{\rm [Cond]}}
  \;\Rightarrow\;
  \underbrace{\text{Prop.}~\ref{prop:a2-from-lemmas}}_{{\rm [Cond]}}
  \;\Rightarrow\;
  \underbrace{\text{Thm.}~\ref{thm:xvii-main}}_{{\rm [Cond]}}.
\]
\end{remark}

\begin{remark}[Scope]
Paper~XVII does not prove the full inverse microlocal conjecture;
does not give global bounds on PSWF;
does not address completeness or concentration-of-energy.
\end{remark}

\begin{thebibliography}{9}
\bibitem{PaperXVI}
[Author], \textit{Paper~XVI}, 2026.
\bibitem{Olver1974}
F.~W.~J.~Olver,
\textit{Asymptotics and Special Functions},
Academic Press, 1974.
\bibitem{Zworski2012}
M.~Zworski,
\textit{Semiclassical Analysis}, AMS, 2012.
\bibitem{Hormander1983}
L.~H\"ormander,
\textit{The Analysis of Linear PDE~I},
Springer, 1983.
\bibitem{CFU1957}
C.~Chester, B.~Friedman, F.~Ursell,
Proc.\ Cambridge Philos.\ Soc.\ \textbf{53} (1957), 599--611.
\bibitem{MelroseU1979}
R.~Melrose, G.~Uhlmann,
Comm.\ Pure Appl.\ Math.\ \textbf{32} (1979), 483--519.
\bibitem{Slepian1961}
D.~Slepian, H.~O.~Pollak,
Bell Syst.\ Tech.\ J.\ \textbf{40} (1961), 43--63.
\end{thebibliography}

\end{document}
